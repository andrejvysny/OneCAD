# PLAN: Fix and harden the 2026-09-14 real-user UX review (59 findings, 8 blocking) — Shapr3D as the UX standard
status: ready
criticality: critical (regen identity, solver truth, protocol changes inside)
run shape: sequential work packages; ≤3 implementers in parallel inside a package on file-disjoint groups; worktrees off; Workflow tool no; Astra pre-approved list (6 calls, fan-out ≤2)

> Copy this file to the repo root as `PLAN.md` (replacing the finished 2026-09-11 plan) when the run starts; keep the Run log there.

## Context

Source: `docs/qa/UX_REVIEW_2026-09-14.md` + the published HTML report (`docs/qa/UX_REVIEW_2026-09-14.html`, both untracked, commit with WP-0). Two cold-start real-user sessions through the `tauri-agent` harness; 52/52 re-run findings reproduced, 4 revised (S3, V2, T2, T3), 3 new (S15, T8, N12). Reference standard: Shapr3D (see `docs/design/SKETCH_UX_RESEARCH.md` for the cited conventions).

Every mechanism below was traced to source this session (three scouts + main-thread spot checks on the load-bearing claims). Line numbers are current at HEAD `f438ced0`.

### B1 — sketch on a face does not follow the face (SYSTEMIC)
- `onecad-core/src/sketch/mod.rs:77-102` `SketchAttachment::HostFace { face: ElementRef, projected_boundary_version }` is core-owned; `Sketch.plane` (`plane.rs:39-50`) is four raw `Vec3`s, "frozen with the sketch" (MODEL-OPS W2, `api/mod.rs:2621`, `document_runtime.rs:6743-6763 plane_ref_of`).
- The timeline record `SketchOpParams` (`document/record.rs:1252-1284`) carries `plane` + `host_face: Option<ElementRef>`; `wire.rs:449-468 strip_sketch_host_face` removes `hostFace` before the wire (SCHEMA §7.3 :1140-1176, §14 2026-07-26 entry :7099-7109). Worker `PlanExecutor.cpp:429-434` materialises a `Sketch` step from raw params — no resolution, never `NeedsRepair`. `resolve_datum`/`set_resolved` (`document/datum.rs:125-233`) are likewise unwired.
- **Precedent to copy:** PlaceComponent mate reseat — SCHEMA §7.2 `planStep.matePlacement` (:698-710) and §7.3 "Re-seated by the worker on every regen since P3 WP-3.1" (:2760-2800): target re-resolved mid-plan through the ladder, coincident seat = frozen point projected onto the resolved plane along its normal, echoed back as a derived no-undo writeback committed inside the same `finish_regen` (`executor.rs:259-312 mate_placement_by_step`, `document_runtime.rs:2600 sync_mate_placements`, `wire.rs:906-917`), unresolvable ⇒ §9 item and the frozen placement stands. The identical defect class was already fixed there ("5→8 mm plate left its screw 3 mm in the air with no diagnostic", finding component-gear-0; fixture `place_component_mate_reseat.ndjson`).
- No-effect Cut: `feature_pattern_same_topology_set` (`FeaturePatternIdentity.cpp:67-88`) runs only on the FeaturePattern shared-host lane (`FeaturePattern.cpp:446-459`); ordinary Extrude Cut has no check. The Add side already REFUSES a disjoint tool (`extrude_add_disjoint_refusal.ndjson`, "Extrude Add: the tool does not touch the target body") — the precedent for the Cut rule.
- N9 "New body created": `completionHint.ts:12-30` keys off `BooleanMode` only; re-edit reuses `finishExtrude` (`ModelToolController.ts:9293-9348, 10275-10308`).
- No Rust/worker/e2e test re-edits a host extrude and checks the dependent sketch (`src-tauri/tests/sketch_on_face.rs`, `e2e/sketch-on-face.spec.ts` checked).

### B2 — clean status on violating geometry (SYSTEMIC)
- `document_runtime.rs:4182-4224 sketch_upsert_with_outcome`: "A non-drag upsert is an identity solve (no coordinate write-back — the worker's `SketchUpsert` reports no positions)". Only `end_gesture_with_outcome` (:4354-4386) applies solved geometry, through `EditCommand::SketchDragGesture { before, after }` (one undo entry). Core already has `Sketch::apply_solved_positions` (:583) and `apply_solved_curves` (:614); `wire.rs:2428-2455 parse_sketch_upsert` already parses `positions`/`curves` when present.
- Worker `SolverLane.cpp:415-457 on_upsert` runs a real PlaneGCS solve and writes the SOLVED coordinates into its own store (:433-435) but the wire result carries only `dof/state/conflicting/maxResidual/entityStates`. Worker store ≠ Rust document from the first inferred constraint on: `SketchRegions` and gestures see solved geometry; the timeline `Sketch` record and the extrude see the authored (±5° `HV_TOLERANCE_RAD`, `autoConstrain.ts:244-257`) geometry → "the extruded solid is a skewed prism".
- `maxResidual` is REPORTING ONLY (SCHEMA §7.4 :2941-2960) and the schema pre-announces the gate: "a future per-dimension gate (length residual ≤ `SolverConfig::tolerance` = 1e-4 mm, angular residual ≤ a named angular tolerance) is a recorded follow-up that would land only with a reproduced local-minimum case and its own §14 entry". B2 is that case. Fixture `sketch_solve_residual.ndjson` pins reporting-only today.
- PlaneGCS `GCS.cpp:5600-5641 diagnose`: a rank-dependent constraint is REDUNDANT only when its error at the solved subset is `< convergenceRedundant`; otherwise it stays in a conflict group → `Conflicting`. On never-solved geometry that is why B3's redundant set reads `Conflicting` rather than `OverConstrained`.

### B3 — Line tool authors Perpendicular on top of H/V
Two independent authoring paths merge without semantic dedupe: coordinate inference `autoConstrain.ts:568-590` (H/V excludes Perp inside one function) and snap-intent persistence `snapPersistence.ts:129-192` (polar-ray `Perpendicular` candidate from `snapCandidates.ts:1226-1285`, only reachable mid-chain via `SketchController.ts:2061-2081`). `buildCommitConstraints` (:2823-2905) dedupes by `canonicalKey` (exact signature) so `Vertical(b)` and `Perpendicular(a,b)` never collide. The Rectangle tool has no pending anchors → no polar candidate → clean set. `mockConflicts.ts` R5 already holds a signed union-find over H/V/Parallel/Perpendicular direction chains — the rule to reuse.

### B6 — dragging a welded rectangle corner fails and corrupts
`Sketch.cpp:1310-1368 beginPointDrag`: the "rectangle strategy" pins the opposite corner only if `getOrderedBoundaryPointIds` (`RegionUtils.cpp:366-456`) finds four corners by POINT-ID identity (`sharedEndpoint` :394-402); Coincident-welded corners fail it → fallback pins EVERY other point (:1350-1356). The cursor drive (tag −1, `ConstraintSolver.cpp:778-839`) against frozen H/V neighbours is infeasible → `conflicting` per frame (`SolverLane.cpp:750-813`); `on_end` (:817-897) computes the final status with the pins still active (:839 before :846) and never re-solves unpinned; FE `finishDrag` (`SketchController.ts:3957-4011`) stores that status with no follow-up upsert. Hint text at `SketchController.ts:3886-3893`. Group drags keep their own path (`beginGroupDrag`).

### B4/B5/B7/B8 — 2D fillet and trim (`src/tools/sketch/sketchService.ts`)
- Trim :405-423 blanket-drops every constraint naming the trimmed entity (B7); Extend :492-512 `remapExtendConstraints` is the selective-remap precedent.
- Fillet :679-740 authors two trimmed legs + a tangent arc (`sketchFilletMath.ts:131`) and ONLY two `Coincident` welds (:714-731); no Radius (B8); Tangent deliberately omitted because the entity-level PlaneGCS Tangent is degenerate at a welded endpoint (`test_endpoint_weld_makes_entity_tangent_redundant`; "endpoint-tangency constraint kind backlogged", CURRENT_STATE MODELING-REACH W0b). Delete :340-369 is a generic cascade; nothing restores the corner (B5).
- B4 error site `SolverLane.cpp:963-976` (`fill_region` → `!fill.complete`), ear clipper `PolygonFill.cpp:223-275` fails closed on a stalled ear (reflex/degenerate test :242). No worker test covers a fillet-arc profile; hypothesis (unproven): arc discretisation against very short trimmed legs produces near-duplicate/collinear vertices.

### Snapping and sketch feedback (S1–S15)
- S1: the status-bar U/V readout is a SEPARATE raw raycast (`ViewportRoot.tsx:676-699` → `viewportStore.setCursor`), never `SnapDecision.point`; grid reach `snapTypes.ts:302-347` (0.42 cell, 4 px floor); TODO session-31 follow-up: "snap arbitration makes grid snapping inoperative after the first click of a gesture (unpinned)".
- S2: badge label = arbitration winner (`snapArbitration.ts:296-420`, hysteresis 2 px / 2 frames); authored constraint = post-commit coordinate match (`autoConstrain.ts:519-702`); an onCurve candidate clamps to the segment endpoint (`snapCandidates.ts:1058-1073`) so an endpoint can read "On Curve" while Coincident/Midpoint is authored.
- S3: no close-loop affordance (`closedLoop.ts` post-commit only). S4: `bumpInteraction()` (`SketchController.ts:2016-2021`) never clears the indicator; the settings subscriber (:646-654) and Alt (:4212, :4337) call it alone; `SketchController.snapClear.test.ts` covers four paths only. S5: three near-identical settings (`snapTo.sketchGuideLines`, `snapTo.sketchGuidePoints`, display-only `show.guidePoints` `ViewportRoot.tsx:534-551`); guides render only inside the release radius. S6: typing is claimed only with ≥1 anchor (`SketchController.ts:2679-2694`) and the chip repaints on the move loop. S7: locks apply on Enter/Tab only. S8: `LiveDimField.tsx:233-238` drops an invalid parse on Tab silently; `liveDimension.ts:265-270`. S9: `annotationPlacement.ts` + `HtmlOverlayDriver.ts:502-539` collision layout exists; only `MeasureOverlay.tsx` opts in. S12: `InspectorPanel.tsx:192-221` never reads `sketchSelectionStore`. S13: `toolStore.ts:142` defaults to `"line"` on every `setMode("sketch", id)`. S14: `SHOW_DEFAULTS.coincidentBadges=false` is a recorded deliberate change (keep); select has no `TOOL_MACHINES` entry (`SketchController.ts:1728-1729`). S15: `units/format.ts:101-115` trims trailing zeros.
- S10/S11: `ConstraintList.tsx:66-107` renders the raw type; `constraintCatalog.ts` labels only reach badges; "Over-constrained · DOF n" (`constraintStatus.ts:28-43`) is PlaneGCS's benign-redundancy state (SCHEMA §7.4 :3011-3020).

### Picking, camera, chips, naming, chrome
- T8: one pick path for hover and click (`Picker.ts:444-502`, `choosePreferredHit` :119-129); edge wins only if `edgeHit.distance ≤ faceHit.distance + bias` with `bias` a perspective-FOV-dependent WORLD margin (:74-89) — camera-angle sensitive, matching the "inconsistent, not impossible" observation. `HighlightLayer.ts:185` already highlights edges. `toolApplicability.ts:154-189`: fillet requires an edge (:164-165), hole/measure fall to `default: ENABLED` (C6), combine needs one selected body.
- V1: `CameraRig.ts:48-50` fov 76 (`ViewportEngine.ts:277`), iso yaw/pitch defaults `CadOrbitControls.ts:183-184,239-241`. V2: `cameraFit.ts:35-82` margin 1.15; safe rect = `largestClearRect` (`viewportWorkAreaStore.ts:63-93`) — a maximal rectangle, asymmetric by construction; bounds walk `bodiesRoot` only (the triad is NOT included — the review's guess was wrong). V3: `fitView` :1219-1227 fits sketch geometry only; tint quad `SketchObject.ts:352-365` opacity 0.5. V4: `orientationAlongNormal(normal, xAxis)` (:1442, `CadOrbitControls.ts:158-162,598-626`) aligns sketch-local X to screen-right and the XY plane basis maps sketch X → world +Y (`plane.rs:57-64`, persisted, must not change). V5: `ViewportRoot.tsx:660-717` has no `pointerleave`.
- T1 `toolChipPlacementStore.ts:16-19` + `ModelToolController.ts:1646-1647` (centroid anchor). T2 `ExtrudeChipControls.tsx:144-161` native `title`; app tooltip `src/ui/Tooltip.tsx`. T4 `ViewportEngine.ts:1898-1906`. T5 `ActiveToolTargets.tsx:146-150` ← `this.normal` set at arm only (:1648, :2633). T6 `ModelToolController.ts:1755-1760`. T7 `depthProjection.ts:62-66 snapDepth` unused. N12 `dto.rs:2180-2185 .abs()`.
- N1/N2 `regen/engine.rs:585-604` Display "op failed ({code:?}, recoverable=..): {message}", `error.rs:65-93,128-170`, `tauriClient.ts:429-443`, `StatusBar.tsx:83-104`. N3/N4/N11 `ActiveToolTargets.tsx:22-33,90-99`, `activeToolPresentation.ts:104-120`. N5 `measurePresentation.ts:9-12`. N6 `paletteItems.ts:43-48`. N7/N8 `ActiveToolInspector.tsx:259-298`. N10 `constraintStatus.ts:15-25`, `InspectorPanel.tsx:293-299`.
- C1 `Popover.tsx:143-172` + `SnapPopover.tsx:104-113` (no `autoFocus`) vs `NavigationHelp.tsx:22`. C2 `Popover.tsx:66-80,234-235`. C3/C4/C5 `Tooltip.tsx:17-59` (zero delay, z-[60] vs popover z-[100]). C8 `edit/undo.rs Inverse::RestoreSketchVisibility`, `treeActions.ts:134-155`. C9 `fileActions.ts:83-95`. C10 `HistoryList.tsx:314,367-368,455-479`. C11 `ToolButton.tsx:61`, `modules/library/register.ts:58,71,84`.

### Repo state and gate health
- Branch `master` at `f438ced0`; untracked: the two review docs. Worker staged sha `e1ebc47a…` == built, 197 ctest targets, OCCT 8.0.1 at `~/.onecad-occt/8.0.1`, codex-cli 0.154.0.
- HEAD's e2e lane was never measured green after session 31: `commitFillet` same-turn race (`e2e/chamfer-angle.spec.ts:129/:153`, `ModelToolController.commitFillet` ~:9501-9512 awaiting `chamferSync`) is OPEN and was "the last red before Gate A"; last full run 438/106 pre-reconciliation, W1/W2 reconciled agent-run to ~60/62.
- Session-32 harness ledger also records: intermittent first-Extrude `applyEdit … correlation TIMED OUT` rollback; "left-click on a body top face selected nothing (edge click did)" — the inverse of T8, same arbitration.
- Conventions/commands verified: bun + vitest, Playwright mock lane port 4177 `retries: 0`, Rust from `src-tauri/` with `ONECAD_REQUIRE_WORKER=1` and the staged worker, ctest, hex gate, QA verifiers; contracts in `src/test/contracts/` are frozen; `settingsStore` persisted version 11.

## Scope

**In:** all 59 findings (B1–B8, S1–S15, V1–V5, T1–T8, N1–N12, C1–C11), grouped into WP-0 … WP-8 in the review's priority order; the chamfer race (WP-0); the endpoint-tangency constraint kind (WP-3b, user decision); the four product decisions below.
**Out (Follow-ups):** re-projection of a host face's `referenceLocked` boundary after the host changes shape (stale-projection banner already exists from WP-P); datum planes following their host (`resolve_datum` unwired — same mechanism, separate WP); per-entity DOF counts; expression input in live chips; the session-32 extrude correlation timeout; export/import; Loft/Sweep.

## Decisions

- **D1 Sketch host-face tracking mirrors the mate reseat exactly** (worker resolves at the `Sketch` step, echoes `planStep.sketchPlacement`, Rust adopts as a derived no-undo writeback inside `finish_regen`; unresolvable ⇒ §9 `NeedsRepair` on the sketch step, frozen plane stands, never dropped, never silently moved). Rejected: Rust-side two-phase regen (breaks the single `ExecutePlan` transaction and checkpoints); FE-side re-projection (FE is never a geometry authority).
- **D2 A Cut that removes no volume is REFUSED**, symmetric with the existing Add-disjoint refusal (`OP_FAILED`, reasonCode `CUT_NO_EFFECT`, evidence `{volumeBefore, volumeAfter, tolerance}`); WP-D1 isolation keeps unrelated bodies. Rejected: warning-only (the review's minimum; inconsistent with the Add precedent and with "a failed feature must not report success").
- **D3 `SketchUpsert` reports solved `positions`/`curves` (additive) and Rust adopts them into the document on every upsert, inside the SAME undo entry as the edit that triggered it** (refresh-only upserts adopt as their own entry, "Resolve sketch"; sketch sessions already squash to one net step at exit). Consequence recorded: a legacy document whose authored geometry violates its inferred H/V moves onto the constraints on first open — that is the constraints finally being true. Rejected: keeping the identity solve and only reporting residuals (the extruded solid would stay wrong).
- **D4 Residual gate becomes a decision** (the SCHEMA §7.4 pre-announced follow-up): a converged solve whose per-constraint residual exceeds its own-dimension tolerance reports `Conflicting` with `conflicting` = the violating constraints (named culprits). Tolerances and the angular scaling law come from Astra A2; `solverPolicyVersion` bump ruled by the protocol-auditor.
- **D5 Drag policy = drive-only**: the dragged point is the only tag(−1) drive, every committed constraint stays active, nothing else is pinned; PlaneGCS solves from the current pose (FreeCAD/SolidWorks behaviour). `on_end` derives status from a fresh unpinned exact solve after the drive is released. Group drag unchanged. Rejected: repairing the rectangle strategy (a heuristic that only ever worked for identity-shared corners).
- **D6 Semantic redundancy dedupe at commit**: an authored `Perpendicular`/`Parallel` intent is dropped when both lines are axis-locked (directly or through the H/V/Parallel/Perpendicular direction chain — the union-find R5 rule lifted out of `mockConflicts.ts` into a shared pure module). Shapr3D refuses redundant adds; we never author them.
- **D7 Trim remaps, never silently drops**: Radius/Diameter carry to every surviving arc piece; positional relations remap to the piece that keeps the position; the rest are dropped WITH a hint naming them. Fillet authors `Radius` (typed value) + endpoint tangency (WP-3b). Deleting a fillet arc restores the corner when both welded legs are tangent to it and non-parallel (Shapr3D/Fusion behaviour).
- **D8 Snap feedback has one writer**: `SketchController` publishes the per-frame `SnapDecision` (or null) to `viewportStore`; the indicator, the badge and the status-bar readout all read it; the raw raycast readout is the fallback only when no decision exists. Invariant (tested): indicator ≡ last published decision or hidden.
- **D9 Edge picking arbitrates in screen space with a depth tolerance scaled by pixel size at the hit depth** (zoom- and FOV-invariant), replacing the world-distance bias. Fillet/Chamfer enable for a FACE selection (expanded to that face's edges at arm); body selection stays disabled with the reason "Select edges or a face" (user, 2026-09-14).
- **D10 Default camera: perspective, FOV 35°, same three-quarter iso framing** (user). Fit centres in a symmetric chrome-inset rect (not the largest clear rectangle). Face-sketch entry pads 1.6× and drops the plane tint to 0.22. TOP of a WORLD plane shows world +X right / +Y up (camera only; persisted bases untouched) and the readout labels X/Y there.
- **D11 Visibility toggles leave the undo stack** (user): `SetVisibility` becomes a view-state transaction that records no undo entry; still saved with the document. Recorded as a user-visible decision in `TODO.md`; `e2e/model-undo.spec.ts`/`tree-visibility.spec.ts` expectations change with it.
- **D12 Re-editing an Extrude exposes end condition, symmetric and boolean mode**; changing to/from NewBody may cascade `NeedsRepair` downstream — deterministic, acceptable, hinted.
- **D13 User-facing error layer**: `EngineError::OpFailed` Display = the message only (code/recoverable stay structured); `toClientError` stops prefixing the kind; the status bar truncates to one line with the full text on hover; region hashes never reach a sentence (reasonCode copy table).
- **D14 Contracts:** `INSPECTOR_SECTIONS_CONTRACT.sketchMode` gains an "Entity" section (S12) — a recorded user-visible decision, not a probe fix. No other contract edits; `modelingInteraction.golden` must stay green untouched.
- **D15 Astra pre-approved** (user): A1/A2/A3 `derive` at xhigh, one `break` at xhigh after each kernel/identity diff (WP-1, WP-2, WP-3); fan-out ≤2; grounded on ≤12 named paths; no `ultra`. Each run's approval summary goes in the Run log.
- **D16 Commits** (user): one commit per green gate boundary on `master`, never push; ledger (`TODO.md`) in the same commit. Five commit boundaries: WP-0 · WP-1+WP-2 · WP-3 · WP-4+WP-5 · WP-6/7/8.

## Plan-review amendments (2026-09-15, fresh-context reviewer-critical, verdict fix-required — all accepted)

- **A-1 (blocker) NeedsRepair path.** `PlanExecutor.cpp:714` skips `run_single_op` when the generic `inputs[]` pre-flight left repair items, and `:560` marks a body-less step with repair items `NeedsRepair`. Therefore `hostFace` stays OUT of `inputs[]` (the `mate.target` rule), the `Sketch` branch resolves it itself and returns its repair items through its own `OpOutcome`, the step reports `needsRepair`, and the plan halts at m−1 (WP-D1 isolation applies). D1's promise is restated: the SKETCH stays visible at its authored frame because Rust owns the sketch document state (the projection is not regen output); the downstream features halt deterministically instead of cutting at a wrong plane. The fixture/tests assert the halt, not a "published at z=40" step.
- **A-2 (blocker) Evidence channel.** `element_refs_mut` gets a `Sketch` arm returning `host_face` as its only entry (index 0 → repair `refId "<opId>.input0"`, the mate addressing precedent; the plan's `<opId>.hostFace` form is withdrawn). `stamp_intents` therefore stamps `intent.descriptor` on mint / re-edit only (`finish_sketch` re-mints the record anyway — its hash moves on every finish); records loaded from disk stay unstamped and resolve through the tracked rung or refuse deterministically. `regen_planner` goldens must stay byte-identical (they never pass through the funnel) — asserted in 1.3's verify.
- **A-3 (blocker) Cut-no-effect scope.** D2 applies to EVERY subtracting op tail (`publish_boolean_result`: Extrude Cut, Revolve Cut, Boolean subtract). `worker/tests/test_revolve_boolean_modes.cpp:203` ("revolve-cut-miss: volume unchanged … removes nothing") legitimately flips to assert the refusal — recorded decision. Task 1.2 verify becomes `ctest -R 'sketch_host_face|canonical_sketch|extrude|boolean|revolve'`.
- **A-4 (blocker) Upsert sites.** D3 enumerated: `sketch_upsert_with_outcome` (:4221) ADOPT, coalesced with the edit into one undo entry and ONE `CommandOutcome` spanning both (A-5); `finish_sketch_with_outcome` (:4535) ADOPT before the timeline record is minted — this is where B2's skewed prism is fixed; `enter_sketch` (:4055) ADOPT, and the solve moves AFTER `undo_watermark` is taken so `squash_sketch_session` collapses it; `begin_gesture` (:4254) NO adoption (gesture lane; EndGesture adopts); `PreparedSketchRegions::drive` (:389, clone, lock released) NO adoption. "Resolve on document open" is withdrawn — nothing upserts at open; the visible move happens on sketch ENTRY of a legacy sketch.
- **A-5 (major)** The coalesced apply returns a single `CommandOutcome`; a test asserts the regen scheduler sees the solved geometry.
- **A-6 (major) Convergence semantics.** `SolverConfig::tolerance` feeds `setConvergence`, which PlaneGCS compares to the squared STEP norm (`GCS.cpp:2018, 2080-2085`): `Converged` says nothing about residuals; `ConstraintSolver.cpp:434 success = (Success || Converged)` is the B2 conflation itself. The residual gate must use its own per-dimension tolerances (A2 output), never `SolverConfig::tolerance` as a residual bound; the A2 run is told this by `followup` if its answer leaned on the §7.4 quote.
- **A-7 (major)** The gate enumerates covered constraint kinds; an unreadable/uncovered kind is reported UNMEASURED (state falls back to the rank diagnosis + an `info` diagnostic), never "satisfied"; a ctest pins that adding a kind without a residual fails.
- **A-8 (major)** A fixture round pins the gate's POSITIVE case (a `Converged`-with-large-residual local minimum, the A-182 rectangle) with the new verdict and the culprit ids.
- **A-9 (major) Authored frame immutable.** The record's `plane` stays the AUTHORED frame (never overwritten); the transported frame is DERIVED document state `Sketch.resolved_plane: Option<SketchPlane>` (beside `projected_boundary_version`), published as the effective plane in the projection, not in the record and not in any hash. The worker computes the transport from (authored frame, resolved face) on EVERY regen — a pure, idempotent function (A1 must prove idempotence) — and echoes `sketchPlacement` whenever effective ≠ authored beyond the epsilons; Rust stores it as derived state and publishes a change only when it differs from the stored resolved plane. Undoing the host edit therefore returns the sketch to its authored frame with no stale intermediate.
- **A-10 (major) Build serialisation.** Only one agent at a time may run `scripts/build-worker.sh` or `ctest`; 3.1 depends on 2.6; groups B and C never build the worker concurrently (the orchestrator sequences the worker halves 1.2 → 2.2).
- **A-11 (minor)** 2.3 verify: `ONECAD_REQUIRE_WORKER=1 cargo test --test sketch_edit --test sketch_conflicts --test sketch_direct_manipulation` (they live in `src-tauri/tests/`) plus `cargo test -p onecad-core --test edit_session --test regen_planner`.
- **A-12 (minor)** D5 is `DragKind::Point` only; ArcEnd/Radius/EntityBody keep their explicit pin sets; the `on_begin` comment (:568-577) is updated, not deleted; `test_sketch_drag_kinds.cpp` must NOT change, `sketch_direct_manipulation.rs` / `e2e/sketch-drag.spec.ts` legitimately do.
- **A-13 (minor)** `directionLock.ts` lifts only the signed union-find; `mockConflicts.ts` R5 stays byte-identical.
- **A-14 (minor)** 1.3 verify adds `cargo test -p onecad-core --test regen_planner` (golden hashes byte-identical).
- **A-15 (minor)** 3.1's first gate is the exact `incomplete triangulation` reproduction from the dumped FE geometry; the fix site is chosen after the repro.
- **A-16 (minor)** 2.4 adds a mock-lane upsert write-back (`localSolver` H/V exact projection already exists) assertion in `e2e/line.spec.ts`: a 1°-off line + Horizontal ends with both endpoints on one y.

## Protocol pre-audit and Astra derive outcomes (2026-09-15) — binding on WP-1/2/3

- **P-1 (audit Q3, FAIL fixed)** repair `refId` for the sketch host is `<opId>.input0` through a new `element_refs_mut` `Sketch` arm (returns `host_face` only); the `.hostFace` form is withdrawn. `parse_input_ref_id` unchanged.
- **P-2 (audit Q2)** `CUT_NO_EFFECT` is catalogued in §7.3 Boolean/Extrude/Revolve prose (NOT the §8 table, which stays 1:1 with `evaluate_publication_policy`), uses `stage: "publish"` like every Boolean refusal, and rides `PlanPrepared.perStepResults[].diagnostics` + `error.detail.diagnostics` (a failed step emits no `planStep`). The info diagnostic on the Sketch step is worker-emitted (the worker alone knows the frame moved) — the §14 entry says so explicitly against the `MATE_AXIS_ADOPTED` Rust-side precedent. Codes per A1: `SKETCH_HOST_RESEATED` (info, stage `"sketch"`), `SKETCH_HOST_NORMAL_REVERSED` (info); repair reasons `sketchHostNonPlanar`, `sketchSeatOffFace`, `sketchFrameIllConditioned`, `sketchFrameInvalid`, `sketchSeatUnmeasurable` (typed `RepairReason` variants + `Unknown` fallback stays); Cut family `CUT_NO_EFFECT`, `CUT_VOLUME_INCREASED`, `CUT_VOLUME_UNMEASURABLE` on every subtracting tail (`publish_boolean_result`).
- **P-3 (audit Q1)** `hostFace` on the wire is additive; `kernelPolicyVersion` stays 1 (fingerprint `0a6a1dce34181289` unchanged); the Rust golden `wire.rs:5084 sketch_host_face_is_dropped_from_the_wire_params` and the `("Sketch", …, 0, 0)` row at `wire.rs:7983` invert deliberately; `hostFace` stays OUT of `inputs[]`.
- **P-4 (audit Q4 + A-9)** with the authored `plane` immutable and the transported frame as DERIVED document state (`Sketch.resolved_plane`), NO planner/prefix hash moves and no checkpoint drifts. The worker recomputes from the authored frame on EVERY regen (A1: "never transport from the previously published frame"); `planStep.sketchPlacement` is emitted whenever the effective frame differs from the authored one (`moved`, exact — no deadband); `significant` (1e-3 mm / 0.01°) is diagnostic evidence only.
- **P-5 (A1 accepted design)** transport = projected origin + projected X (Gram-Schmidt against n1), `τGS = sqrt(DBL_EPSILON)` conditioning guard ⇒ `sketchFrameIllConditioned` (no auto-fallback publish); planarity = surface-type predicate with orientation-corrected normal; the seat witness is the transported frozen ANCHOR (not the origin) classified with `BRepClass_FaceClassifier` at `BRep_Tool::Tolerance(face)` — `OUT` ⇒ `sketchSeatOffFace`, `UNKNOWN` ⇒ `sketchSeatUnmeasurable`; G8 reversed normal is followed and reported; G9 spin about the normal is undetectable (F1 = F0 by definition — plane re-seating, not rigid following). Cut: `εV = a³ + U`, `a = authoring_resolution()`, `U = err_before·V_before + err_after·V_after` from the relative error `BRepGProp::VolumeProperties(shape, props, Eps)` returns; `|ΔV| ≤ εV` ⇒ refuse `CUT_NO_EFFECT`; `ΔV < −εV` ⇒ `CUT_VOLUME_INCREASED`; non-finite ⇒ `CUT_VOLUME_UNMEASURABLE`. Strict host gate: tracked rung, or AutoBind with score ≥ 0.85 AND margin ≥ 0.10 (Ladder.cpp:342 admits anchor-decided binds below margin; the sketch host refuses them). Optional Sketch param `frameTransportVersion: 1`.
- **P-6 (A2 accepted design)** residual gate: `τ_len = 1e-4 mm` absolute; `τ_ang(c) = τ_len / L_c` with L_c the LOCAL operand span (one line: its length; two lines: the longer; endpoint tangency: max(line span, arc radius)); H/V lower to coordinate equalities (mm residual, `τ_len`); accept iff `q_c = r_c/τ ≤ 1`. Classification: rank-conflict group non-empty ⇒ `Conflicting` (reason `rankConflict`); any `q_c > 1` ⇒ `Conflicting` (reason `residualViolation`, culprits = violating ids ∪ group members, tags 0/−1 excluded); unreadable/non-finite ⇒ `unassessed[]` (never satisfied, never blamed); otherwise `OverConstrained` iff benign redundancy, else DOF classification. Wire (§7.4, additive): `positions`, `curves` on `SketchUpsert`; `reasons: {id: "residualViolation"|"rankConflict"}`, `unassessed: [id]`, `maxResidual` kept as legacy reporting; `solverPolicyVersion` 1 → 2 (manifest axis only). Drag (Point kind only): the dragged point's u,v are the only tag(−1) drives; no pins; committed constraints and tag-0 locks outrank; "cursor distance alone is never `partial`"; T9 signed-area branch guard `sign(S0)·S > τ_len·L` with deterministic subdivision, else restore + `partial: branchBoundary`; EndGesture: final target step, remove drives, UNCONDITIONAL released exact solve → diagnosis → gate → fresh `state`/`conflicting`/`dof` (gesture-fixed `entityStates` still echoed). Authoring: parity graph with axis node X (H: a⊕X=0, V: a⊕X=1, Parallel: a⊕b=0, Perpendicular: a⊕b=1) queried WITHOUT the candidate: implied-equal ⇒ suppress (redundant), implied-different ⇒ reject with witness path, disconnected ⇒ author; snap intent outranks incidental inference within one batch; committed H/V never removed silently. Shared pure module `directionLock.ts` (`classifyDirectionCandidate`), used by both lanes; `mockConflicts.ts` R5 byte-identical.
- **P-7 (audit Q6/Q7)** `sketch_solve_residual.ndjson` header rewritten (its "REPORTING ONLY is asserted structurally" paragraph becomes false) and ONE new round pins the positive gate case (converged-with-large-residual, the A-182 rectangle) with `state`, `reasons`, `conflicting`; existing rounds keep their verdicts (content correction, no bump). Solver-lane fixtures are C++-replay + Rust-parse only (the Rust stub has no solver lane) — recorded weakening, accepted.
- **P-8 (audit Q8, FAIL fixed)** WP-3b authors a DISTINCT constraint kind `TangentAtPoint` (21st kind; `entities: [line|arc, arc]`, `positions: [role, role]` naming the shared endpoint per operand; DOF −1); an unknown `type` fails loudly in `WireSketch::translate`. `Tangent` is untouched.
- **P-9 (WP-0.2 measured)** the chamfer race was the chip's `pending` validation, not the arm generation — see the TODO ledger; the arm-generation follow-ups (reopen fence, superseded analysis) are recorded, not fixed here.
- **P-11 (A3 accepted, `docs/design/astra/sketch-fillet-fill-and-tangency.md`)** Fill: NO positive geometric ear cutoff (a simple polygon can have an essential ear of arbitrarily small positive area); orientation = filtered predicate with bound `ε_area = γ₈·S` (γ₈ ≈ 8.88e-16, S = |x_x y_y| + |x_y y_x|) and an EXACT determinant-sign fallback for `|D| ≤ ε_area`; `point_in_triangle` stays inclusive, expressed through the same predicate; pre-pass = remove exact closure duplicates, certified merges only with provenance (never transitive proximity clustering, never across rings), exact forward-collinear chains (`orient = 0 ∧ (b−a)·(c−b) > 0`) simplified TEMPORARILY and restored by subdividing the adjacent triangle (indexed boundary edges preserved); post-fill verification (T = N + 2H − 2 with bridge occurrences, positive triangles, required boundary edges single-use, every interior/bridge edge two-incident, area equality, f32 encoding check); refusal codes `FILL_*` (OP_FAILED, evidence-bearing). Tangency: `TangentAtPoint` lowers to `addConstraintAngleViaPoint(curve1, curve2, T, &α, userTag)` with α ∈ {0, π} chosen ONCE at the seed from the actual PlaneGCS normals and frozen for the solve/gesture (stable storage, excluded from unknowns), residual in RADIANS, arity 1, requires a structural weld (never nearby coordinates), complete arc construction (do not rely on `makeArc`'s unset Start/End), degeneracy refusals `TANGENT_*`; entity-level distance tangency is first-order singular at a welded corner (proved) — that is why the fillet header's omission was right for `Tangent` and wrong as a policy. Frontend fillet authors atomically: 2 Coincident + 2 TangentAtPoint (matching endpoint roles) + Radius(typed) + remapped survivors; trim remaps by provenance (endpoint tangency/weld only to the piece keeping that exact endpoint; consumed contact ⇒ drop the tangent with a hint; radius intent to every surviving piece of the same support; never drop a real short boundary because of a screen-scaled threshold). B4 attribution stays LOW until the exact repro (A-15); A3's most plausible mechanism is a sub-tolerance closing-seam mismatch (the assembler dedupes consecutive vertices at ≤ tolerance but never compares last vs first; the fill's closure check is 1e-12).
- **P-10** A3 (fillet fill + endpoint tangency) is in flight; WP-3 tasks 3.1/3.2 wait for it.

## Design

### WP-0 — Baseline and the chamfer race
1. Full L3 on untouched HEAD, serial, alone (gate-verifier, then main-thread re-run of anything red): worker build+stage · ctest · fmt/clippy (1.97 + newest stable scratch dir) · `ONECAD_REQUIRE_WORKER=1 cargo test --workspace --no-fail-fast` · tsc · build · vitest · hex · verifiers · `bun run e2e` both projects. Every red recorded with attribution before any fix.
2. `commitFillet` race: red-first vitest that types an angle and confirms in the same turn; fix = the post-await re-check distinguishes "genuinely superseded" from "our own `runChamferSync` reopened the preview" (carry the arm generation across the sync's close/reopen). Then `chamfer-angle` + `filletChamfer` specs ×5 alone.
3. Commit boundary 1: review docs + race fix + ledger.

### WP-1 — Sketch host-face tracking + Cut-no-effect (B1, N9) — critical, protocol
**Astra A1 `derive` first (xhigh, grounded).** Deliverables: the frame-transport rule (given frozen `F0=(o0,x0,y0,n0)` and the resolved face plane `(p1,n1)`: orient `n1` for continuity, `o1` = `o0` projected onto the plane along `n1`, `x1` = Gram-Schmidt of `x0` against `n1`, `y1 = n1 × x1`, with the fallback when `x0 ∥ n1`), the "moved" epsilons with their scaling law (translation mm, rotation rad — same family as `kMateReseatTranslationEpsilonMm`), the degeneracy taxonomy (face rotated about its normal, flipped orientation, split into several coplanar faces, non-planar after a draft edit, face vanished, ambiguous twin), test vectors, and the Cut-no-effect volume epsilon scaled from `precision_of(target).authoring_resolution()` and the body's extent. Grounded paths (≤12): `worker/src/ops/ComponentOp.cpp`, `worker/src/session/FaceProjection.cpp`, `worker/src/session/PlanExecutor.cpp`, `worker/src/session/ElementIdentity.cpp`, `worker/src/elementmap/Ladder.cpp`, `worker/src/ops/ExtrudeOp.cpp`, `src-tauri/crates/onecad-core/src/sketch/plane.rs`, `src-tauri/crates/onecad-core/src/sketch/mod.rs`, `protocol/SCHEMA.md` (§7.2, §7.3 Sketch/PlaceComponent, §9, §10). Never `corpus/expected-values/`.

**Wire (protocol-auditor before and after):**
- §7.3 `Sketch` params: OPTIONAL `hostFace` — the same typed semantic-ref shape as Extrude `targetFace` (identity + evidence + anchor). Rust stops stripping it (`wire.rs:449-468`). `derive_inputs` unchanged (the edge already exists). Hash: `host_face` is already inside `params` on every record minted since VF-B5a, so no planner-hash movement; legacy records without it stay frozen (no backfill — the VF-B5a discipline).
- §7.2 `planStep`: OPTIONAL `sketchPlacement: { plane: {origin,xAxis,yAxis,normal}, translationMm, rotationDeg }` — present only on a `Sketch` step whose host resolved AND moved beyond the epsilons. Absent otherwise (wire byte-identical for every other document).
- §7.2 `diagnostics`: `info` code `SKETCH_PLANE_FOLLOWED_HOST` (stage `"sketch"`, evidence `{translationMm, rotationDeg}`) on the same step; `error` reasonCode `CUT_NO_EFFECT` (stage `"boolean"`, evidence `{volumeBefore, volumeAfter, toleranceMm3}`) on an `OP_FAILED` Cut.
- §9: sketch-host items use `refId: "<opId>.input0"` (element_refs_mut index; A-2), ladder reasons unchanged; op-built reason `hostFaceNotPlanar` added to the closed list (with the `RepairReason` variant — the auditor's standing recommendation).
- §14 entry; new fixture `protocol/fixtures/sketch_host_face_reseat.ndjson` (rounds: follow on a 40→25 edit; NeedsRepair when the host vanishes; not-planar refusal; Cut-no-effect refusal). Additive shapes → no fixture bump, fingerprint unchanged (auditor confirms).

**Worker:** `PlanExecutor.cpp` Sketch branch: when `params.hostFace` is present, resolve it through the same ladder entry the mate uses (not via `resolve_input_refs` — an unresolved host must not zero the plan's bodies), classify planar, transport the frame per A1, rewrite the materialised `params.plane` (local (u,v) geometry verbatim), emit `sketchPlacement` + the info diagnostic when moved; unresolvable/ambiguous/non-planar ⇒ the step reports `needsRepair` with the §9 item and the frozen plane stands (plan stops at m−1, WP-D1 isolation applies). `ExtrudeOp.cpp`/`BooleanOp.cpp` Cut path: `BRepGProp::VolumeProperties` before/after on the target; `|ΔV| ≤ εV` ⇒ `OP_FAILED CUT_NO_EFFECT`.

**Rust:** `executor.rs` collects `sketch_placement_by_step` exactly like `mate_placement_by_step`; `document_runtime.rs::sync_sketch_placements(executed)` writes `document.sketches[id].plane` AND the record's `plane` (derived, no-undo, inside `finish_regen`, prefix hashes recomputed in the same commit as `sync_mate_placements`); `Superseded` discards it. `RepairReason::HostFaceNotPlanar` + parse. Projection carries the new plane (already). FE: history row shows the info diagnostic ("Sketch 2 followed its face, −15 mm"); repair panel already renders §9 items.

**N9:** `completionVerb(op, mode, bodyCount, edited)` → "Extrude updated" for a re-edit.

**Tests (red-first):** `src-tauri/tests/sketch_on_face.rs`: extrude 40 → face sketch → cut 15 → edit 40→25 ⇒ sketch plane origin z = 25 (1e-9), pocket volume = kernel-exact expected, diagnostics carry `SKETCH_PLANE_FOLLOWED_HOST`, undo restores z = 40; host vanished ⇒ `NeedsRepair` item `<opId>.hostFace`, sketch still published at z = 40; imprint-only cut ⇒ `CUT_NO_EFFECT` refusal. Worker: `test_sketch_host_face_reseat.cpp` (transport vectors from A1, degeneracy classes) + the fixture through `worker_harness` and `canonical_*`. Core: `RepairReason` round-trip. Mock lane parity: `mockClient` face-hosted sketches follow the mock extrude depth edit (`e2e/sketch-on-face.spec.ts` gains "edit host depth → pocket survives"). **Astra `break`** on the derivation + diff, then local `adversarial-reviewer`.

### WP-2 — Solver truth (B2, B3, B6, S10, S11) — critical, protocol (additive)
**Astra A2 `derive` first (xhigh, grounded).** Deliverables: (i) the residual gate — per-dimension tolerances, the angular tolerance's scaling law in terms of the sketch's characteristic length and `SolverConfig::tolerance`, and the classification taxonomy {satisfied, redundant, rank-conflicting, residual-violating, diverged} → wire `state`/`conflicting`; (ii) the drive-only drag policy — formulation of the drive (coordinate drives vs point-to-point), step limiting, when "partial" is the honest answer, proof sketch that the DogLeg update from the current pose leaves uncoupled parameters unmoved, degeneracies (fully free sketch, point welded to a locked reference, arc endpoints); (iii) the direction-lock redundancy rule and whether `Perpendicular` between an axis-locked pair is ever worth keeping; (iv) test vectors including the review's exact 11-constraint rectangle (`A-182`). Grounded (≤12): `worker/src/protocol/SolverLane.cpp`, `worker/src/sketch/Sketch.cpp`, `worker/src/sketch/solver/ConstraintSolver.cpp`, `worker/src/sketch/solver/ConstraintSolver.h`, `worker/third_party/planegcs/GCS.cpp` (diagnose/solve excerpts), `src/tools/sketch/autoConstrain.ts`, `src/ipc/mockConflicts.ts`, `protocol/SCHEMA.md` §7.4, `worker/tests/test_solver_residual.cpp`.

**Wire:** §7.4 `SketchUpsert` result gains OPTIONAL `positions` + `curves` (same shape and delta discipline as `EndGesture`, baselined at the request's coordinates); the `maxResidual` paragraph is rewritten to the gate (D4); `SolveDrag`/`EndGesture` `status` wording for the residual case; §14 entry; fixture `sketch_upsert_writeback.ndjson` (a 1° line + `Horizontal` ⇒ `positions` present, both endpoints on one y, `maxResidual ≤ 1e-4`; the A-182 rectangle ⇒ `OverConstrained` after write-back, never `Conflicting`); `sketch_solve_residual.ndjson` rounds re-pinned to the new decision. `solverPolicyVersion` per the auditor.

**Worker:** `on_upsert` serialises `changed_positions(request, solved)` + `changed_curves`; residual gate after the solve (culprit set = constraints whose own residual exceeds their tolerance); `beginPointDrag` drops both pin strategies (D5); `on_end` runs an unpinned exact solve after `endPointDrag()` and reports from it.

**Rust:** `sketch_upsert_with_outcome`: when the result carries positions/curves, build `after` from the post-edit sketch, apply them, and commit `SketchDragGesture { before, after }` COALESCED into the edit's undo entry (add `EditSession::apply_coalesced` or an `SketchEdit`-with-solved variant — one undo entry is the contract; the implementer picks the smaller diff and pins it with a test). Refresh-only upsert ⇒ its own entry "Resolve sketch". `record_solve` unchanged.

**FE:** `finishDrag` hint copy ("Held by constraints" only when the solved point lags the cursor); `buildCommitConstraints` semantic dedupe through a new pure `src/tools/sketch/directionLock.ts` (lifted from `mockConflicts` R5, both lanes share it); `ConstraintList` rows: "Horizontal · Line 2", "Coincident · Line 1 end – Line 2 start", entity display names by type ordinal (`src/features/sketch/entityNames.ts`, shared with S12), rows are buttons (focusable, Delete key), conflict tint only on `conflicting` ids; status vocabulary: `over` → "Redundant constraint · DOF n", `error` → "Conflicting · DOF n — k constraints cannot be met: …" (`constraintStatus.ts` expectations updated, no golden involved).

**Tests (red-first):** worker `test_solver_residual.cpp` (gate decision + culprits), `test_sketch_drag_kinds.cpp` (welded rectangle corner drag moves the corner and its two neighbours' shared coordinates, `success`, no conflict after release); Rust `sketch_edit.rs` (upsert moves the document's coordinates; single undo entry restores both edit and solve), `sketch_conflicts.rs` (A-182 set ⇒ OverConstrained), `sketch_direct_manipulation.rs` (drag then status honest); vitest `SketchController.draw.test.ts` (4-click rectangle via Line tool authors no Perpendicular), `directionLock.test.ts`, `ConstraintList.test.tsx` (named rows; "does not render raw entity ids" stays true — names, not ids); e2e `sketch-drag.spec.ts`, `dimension-conflict.spec.ts`, `constraint-apply.spec.ts` re-baselined only where the mock lane's `enforceDriving` now matches the real lane. **Astra `break`** + local `adversarial-reviewer`.

### WP-3 — 2D fillet and trim (B4, B5, B7, B8) + WP-3b endpoint tangency — careful FE, critical worker/protocol
**Astra A3 `derive` (xhigh, grounded).** (i) Ear-clip degeneracy classes on arc-discretised loops (near-duplicate vertices, collinear runs, sub-tolerance legs), the pre-pass rule and its epsilon scaling vs the node-merge 1e-6 mm and the authoring resolution, and whether a stalled clip may fall back to OCCT `BRepMesh` on the region face without breaking the single-use-edge ring contract; (ii) endpoint tangency: the PlaneGCS formulation (angle-via-point at the shared endpoint vs distance-based line–arc tangency), DOF accounting (−1 per tangency), degeneracies (zero-length leg, arc sweep floor), interaction with the arc endpoint coupling rules, test vectors. Grounded (≤12): `worker/src/loop/PolygonFill.cpp`, `worker/src/loop/LoopDetector.cpp`, `worker/src/protocol/SolverLane.cpp` (on_regions), `worker/src/sketch/solver/ConstraintSolver.cpp`, `worker/third_party/planegcs/GCS.h`, `src/tools/sketch/sketchFilletMath.ts`, `src/tools/sketch/sketchService.ts`, `protocol/SCHEMA.md` §7.3 constraints/§7.4, `worker/tests/test_sketch_arc_endpoints.cpp`.

- B4: worker red-first fixture built from the FE geometry (a vitest dumps the exact entity JSON the trim+fillet sequence produces for the review's shape; that JSON becomes `worker/tests/fixtures/…` + a `SketchRegions` NDJSON round); fix per A3 (pre-pass + ε ear test; fallback only if A3 clears it); fail-closed stays.
- B7: `remapTrimConstraints` (D7) next to `remapExtendConstraints`; hint lists dropped kinds.
- B8: fillet authors `Radius` = typed value on the arc + `Tangent` with `positions` naming the shared endpoint on both welds (WP-3b).
- B5: `deleteEntitiesNow` "unfillet" rule (D7): tangent-welded arc between two non-parallel lines ⇒ extend both legs to their intersection, weld with `Coincident`, drop the arc's constraints; hint "Corner restored".
- WP-3b wire: §7.3 `Tangent` gains OPTIONAL `positions` (the shared point) selecting the endpoint form; a worker that ignores `positions` MUST fail loudly (the Coincident-positions precedent); worker `ConstraintSolver` maps it to the A3 formulation; fixture `sketch_tangent_endpoint.ndjson` (DOF −1, drag keeps tangency); `test_endpoint_weld_makes_entity_tangent_redundant` becomes the positive case. FE `autoConstrain` tangent-arc chaining also authors the endpoint form.
- Tests: `sketchService.trim.test.ts` (remap table), `sketchService.fillet.test.ts` (Radius + Tangent authored; DOF), `sketchService.delete.test.ts` (unfillet), e2e `sketch-fillet.spec.ts`/`sketch-trim.spec.ts` gain "finish sketch after fillet → 1 region" and "trim keeps Ø", Rust `sketch_regions.rs` (fillet profile triangulates). **Astra `break`** on A3 + diff, local `adversarial-reviewer`.

### WP-4 — Snapping feedback (S1–S9, S15) — careful FE
- 4a Single writer (D8): `viewportStore.snapFeedback` published from `decisionAt` per frame and cleared by `bumpInteraction`, `clearTransientSnapFeedback`, every commit, tool switch, Escape, blur, settings, Alt; `SnapIndicator` and `StatusBar` read it; readout shows the SNAPPED U/V while a decision is live. Invariant test drives a scripted event sequence and asserts indicator ≡ decision after each event. Grid: red-first vitest for "grid inoperative after the first click" using the review's numbers (20 mm grid, radius M); fix whatever the measurement names in `snapArbitration`/`snapCandidates` (bounded judgment).
- 4b Kinds and labels (S2): coordinate-dedupe before arbitration (an `onCurve` whose point coincides with an endpoint/midpoint/center candidate is dropped); the hysteresis latch yields immediately to a higher-ranked candidate at the SAME point; labels: Endpoint, Midpoint, Center, Quadrant, Intersection, On curve, Grid, Origin; test: label kind ≡ authored relation for the same click.
- 4c Close-loop feedback (S3): decision kind ∈ {endpoint, midpoint, center} draws an accent ring on the target point; when the endpoint is the open start of the current chain the label reads "Close loop".
- 4d Guides (S5): red-first render test (alignV guide at 6 px from an existing x); guide reach = max(acquire radius, 12 px); settings renamed: "Alignment guides", "Snap to sketch points (end, mid, center)", "Show point markers" (no store keys change → no version bump).
- 4e Numeric entry (S6/S7/S8/S15): chip re-renders synchronously on key input; provisional lock previews the typed value on the rubber band while typing (locks-before-quantum path in `liveToolMachines`); Tab keeps the text on an invalid parse and is a no-op on a single-field tool; live chips format with a fixed 2 decimals for mm (`formatChip`, separate from `formatLength`); `live-dim-*` e2e expectations updated where the display changed.
- 4f Overlay collision (S9): snap hint, live-dim chips, selection dimension labels and constraint badges opt into `annotation` placement with priorities hint > chip > label > badge and the cursor vicinity as a protected rect.

### WP-5 — Picking and enablement (T8, C6, C7) — careful FE
- `Picker.ts`: screen-space arbitration (D9): an edge hit is accepted when its screen distance ≤ `EDGE_PICK_PX` (raised 6 → 8) and its depth ≤ face depth + `k · pixelWorldSize(depth)`; hover and click share it (already one path). Red-first `Picker.test.ts` cases at FOV 76 and 35 with silhouette and interior edges, plus the session-32 "face not selectable" inverse.
- `toolApplicability.ts`: fillet accepts face (reason for body "Select edges or a face"); `ModelToolController` arm expands a face selection to its boundary edges (existing edge-of-face query in `EdgePicks`/`QueryBodyTopology`); hole/measure need ≥1 visible body; combine needs ≥2 bodies; Hole must not arm on an empty document.
- e2e: `filletChamfer.spec.ts` gains "fillet from a face selection"; a new `edge-pick.spec.ts` raycasts real edge positions via `?vpdebug`.

### WP-6 / WP-7 / WP-8 — chips, camera, naming, chrome (T1–T7, V1–V5, N1–N12, C1–C11, S12–S14)
Task table FP-* below (drafted by the Plan agent, reviewed). Product decisions D10–D14 apply. Notable designs: FP-V1 exports `DEFAULT_FOV_DEG = 35` and scales the no-bounds distance 260 → 644 so ortho framing is invariant; FP-V2 adds `fitSafeRect` (symmetric inset) beside `obstacleClearRect` (overlay placement untouched); FP-T4 keeps the L2 body neutral and tints the L1 prism; FP-T2/T3 visible "Symmetric" + "Flip" through `resolveDepth(raw,{flip})`; FP-T7 gives `snapDepth` its first call with the sketch quantum when `snapTo.dimensionRound` is on; FP-N1 changes `EngineError` Display (tests that pin the old string update); FP-C8 variant A; FP-S12 new `SketchEntityProperties` section (D14).

## Tasks

| id | goal | tier | files | design | deps | group | est. lines | verify |
|---|---|---|---|---|---|---|---|---|
| 0.1 | Full L3 baseline on HEAD, every red attributed | standard (gate-verifier) | none | WP-0 §1 | — | A | 0 | the ladder, serial, alone; record counts |
| 0.2 | Fix `commitFillet` same-turn race | careful | `src/tools/modelTools/ModelToolController.ts` ~:9501-9512, ~:4206; new `ModelToolController.chamferSameTurn.test.ts` | WP-0 §2 | 0.1 | A | 80 | new vitest red→green; `bunx playwright test e2e/chamfer-angle.spec.ts e2e/filletChamfer.spec.ts` ×5 alone 0 red |
| 0.3 | Commit boundary 1 | — | review docs, TODO.md | D16 | 0.2 | A | — | full L3 green |
| 1.0 | Astra A1 derive → `docs/design/astra/sketch-host-face-transport.md` | Fable | packet only | WP-1 | 0.3 | B (∥ 2.0) | — | numbers verified by scratch computation + a worker probe |
| 1.1 | SCHEMA §7.2/§7.3/§9/§14 + fixture `sketch_host_face_reseat.ndjson` | critical (+protocol-auditor pre) | `protocol/SCHEMA.md`, `protocol/fixtures/` | WP-1 wire | 1.0 | B | 250 | auditor PASS; fixture parses in both lanes |
| 1.2 | Worker: Sketch-step host resolution, transport, `sketchPlacement`, NeedsRepair, `CUT_NO_EFFECT` | critical | `worker/src/session/PlanExecutor.cpp:420-434`, new `worker/src/session/SketchHostReseat.{h,cpp}`, `worker/src/ops/ExtrudeOp.cpp`, `worker/src/ops/BooleanOp.cpp`, `worker/tests/test_sketch_host_face_reseat.cpp`, `worker/tests/CMakeLists.txt` | WP-1 worker | 1.1 | B | 500 | `ctest -R 'sketch_host_face|canonical_sketch_host|extrude'` green; full ctest |
| 1.3 | Rust: send `hostFace`, collect/adopt `sketchPlacement`, `RepairReason::HostFaceNotPlanar`, wire parse | critical | `src-tauri/src/worker/wire.rs:449-468, 906-917`, `onecad-core/src/regen/executor.rs:259-312`, `src-tauri/src/document_runtime.rs:2231, 2600-2660`, `onecad-core/src/document/repair.rs`, `src-tauri/tests/sketch_on_face.rs` | WP-1 Rust | 1.2 | B | 450 | `ONECAD_REQUIRE_WORKER=1 cargo test --test sketch_on_face --test wire_contract --test topology_rebind` green |
| 1.4 | FE: mock-lane parity + history info row + N9 verb | careful | `src/ipc/mockClient.ts` (host-face sketches), `src/features/inspector/HistoryList.tsx`, `src/tools/modelTools/completionHint.ts:12-30`, `e2e/sketch-on-face.spec.ts` | WP-1 FE | 1.3 | B | 220 | `bunx vitest run src/ipc src/tools/modelTools/completionHint*`; `bunx playwright test e2e/sketch-on-face.spec.ts` |
| 1.5 | Astra `break` on A1 + diff; local adversarial-reviewer; red-first fixes | Fable / reviewer | — | — | 1.4 | B | — | findings ledger in Run log |
| 2.0 | Astra A2 derive → `docs/design/astra/sketch-solver-truth.md` | Fable | packet only | WP-2 | 0.3 | B (∥ 1.0) | — | numbers verified by `ctest -R solver_residual` probes |
| 2.1 | SCHEMA §7.4 upsert positions/curves + residual gate + fixtures | critical (+auditor) | `protocol/SCHEMA.md` §7.4, `protocol/fixtures/sketch_upsert_writeback.ndjson`, `sketch_solve_residual.ndjson` | WP-2 wire | 2.0, 1.1 (serialise SCHEMA edits) | C | 220 | auditor PASS |
| 2.2 | Worker: upsert write-back, residual gate + culprits, drive-only drag, unpinned end solve | critical | `worker/src/protocol/SolverLane.cpp:415-457, 568-642, 750-897`, `worker/src/sketch/Sketch.cpp:1310-1386`, `worker/src/sketch/solver/ConstraintSolver.cpp:778-839`, `worker/tests/test_solver_residual.cpp`, `test_sketch_drag_kinds.cpp` | D3–D5 | 2.1 | C | 450 | `ctest -R 'solver|sketch|canonical_sketch'` green; full ctest |
| 2.3 | Rust: adopt upsert positions/curves in one undo entry | critical | `src-tauri/src/document_runtime.rs:4201-4224`, `onecad-core/src/edit/session.rs` (coalesce), `edit/undo.rs`, `src-tauri/tests/sketch_edit.rs`, `sketch_conflicts.rs`, `sketch_direct_manipulation.rs` | D3 | 2.2 | C | 300 | `ONECAD_REQUIRE_WORKER=1 cargo test --test sketch_edit --test sketch_conflicts --test sketch_direct_manipulation -p onecad-core` green |
| 2.4 | FE: direction-lock dedupe, drag hint copy, named constraint rows, status vocabulary | careful | new `src/tools/sketch/directionLock.ts`, `src/ipc/mockConflicts.ts`, `src/tools/sketch/SketchController.ts:2823-2960, 3886-3893`, `src/features/inspector/ConstraintList.tsx`, new `src/features/sketch/entityNames.ts`, `src/features/sketch/constraintStatus.ts`, `src/ipc/localSolver.ts` (mock write-back parity) | D6, S10, S11 | 2.3 | C | 420 | `bunx vitest run src/tools/sketch src/features/inspector src/features/sketch src/ipc`; `bunx playwright test e2e/sketch-drag.spec.ts e2e/dimension-conflict.spec.ts e2e/constraint-apply.spec.ts e2e/line.spec.ts` |
| 2.5 | Astra `break` + adversarial-reviewer; fixes | Fable / reviewer | — | — | 2.4 | C | — | ledger |
| 2.6 | Commit boundary 2 (WP-1 + WP-2) | — | TODO.md | D16 | 1.5, 2.5 | — | — | full L3 green, serial, alone |
| 3.0 | Astra A3 derive → `docs/design/astra/sketch-fillet-fill-and-tangency.md` | Fable | packet | WP-3 | 2.0 | D | — | probe: fixture region round |
| 3.1 | B4 red-first worker fixture + fill fix | critical | `worker/src/loop/PolygonFill.cpp:223-275`, `worker/tests/test_region_table.cpp` (+ new `test_region_fillet_profile.cpp`), `protocol/fixtures/sketch_regions_diagnostics.ndjson` (new round), a vitest that dumps the FE geometry | A3 (i) | 3.0 | D | 260 | `ctest -R 'region|canonical_sketch_regions'` green |
| 3.2 | SCHEMA §7.3 Tangent positions + fixture; worker endpoint tangency | critical (+auditor) | `protocol/SCHEMA.md`, `protocol/fixtures/sketch_tangent_endpoint.ndjson`, `worker/src/sketch/WireSketch.cpp:348+`, `worker/src/sketch/solver/ConstraintSolver.cpp`, `worker/tests/test_sketch_arc_endpoints.cpp` | A3 (ii) | 3.0, 2.6 | D | 320 | auditor PASS; `ctest -R 'arc_endpoints|tangent'` |
| 3.3 | FE trim remap, fillet Radius+Tangent, unfillet on delete | careful | `src/tools/sketch/sketchService.ts:340-423, 579-740`, `src/tools/sketch/autoConstrain.ts` (tangent-arc chaining), `src/ipc/localSolver.ts`, tests `sketchService.trim/fillet/delete.test.ts`, `e2e/sketch-fillet.spec.ts`, `e2e/sketch-trim.spec.ts` | D7 | 3.2 | D | 380 | `bunx vitest run src/tools/sketch`; the two specs |
| 3.4 | Rust: `Tangent` positions round-trip + regions test | careful | `src-tauri/src/worker/wire.rs:4500-4700`, `onecad-core/src/sketch/mod.rs`, `src-tauri/tests/sketch_regions.rs` | WP-3b | 3.2 | D | 120 | `cargo test --test sketch_regions --test wire_contract` |
| 3.5 | Astra `break` + adversarial-reviewer; commit boundary 3 | — | TODO.md | — | 3.3, 3.4 | — | — | full L3 green |
| 4.1 | Snap feedback single writer + invariant + grid measurement/fix (S1, S4) | careful | `src/tools/sketch/SketchController.ts:1862-2056, 646-654, 4212, 4337`, `src/stores/viewportStore.ts`, `src/viewport/engine/SnapIndicator.ts:361-454`, `src/features/shell/StatusBar.tsx:155-161`, `src/viewport/ViewportRoot.tsx:676-699`, `src/tools/sketch/snapArbitration.ts` | D8, 4a | 2.6 | E1 | 320 | new `SketchController.snapFeedback.test.ts` (invariant + grid) ; `bunx playwright test e2e/sketch-snap*.spec.ts` |
| 4.2 | Kinds/labels dedupe + close-loop ring (S2, S3) | careful | `src/tools/sketch/snapCandidates.ts:940-1090`, `snapArbitration.ts:296-420`, `SnapIndicator.ts` (ring), `snapEngine.test.ts` | 4b, 4c | 4.1 | E1 | 220 | `bunx vitest run src/tools/sketch/snap*`; `e2e/line.spec.ts` "close loop shows Close loop" |
| 4.3 | Guides reach + settings copy (S5, S14 naming) | standard | `src/tools/sketch/snapTypes.ts:288-372`, `src/features/snap/SnapPopover.tsx:22-55`, `src/features/settings/SettingsModal.tsx:43-57`, `SnapIndicator.test.ts` | 4d | — | E2 | 90 | render test red→green |
| 4.4 | Numeric entry (S6, S7, S8, S15) | careful | `src/tools/sketch/liveDimension.ts:253-291`, `src/features/sketch/LiveDimField.tsx:88-106, 233-238`, `src/tools/sketch/liveToolMachines.ts`, `src/tools/sketch/SketchController.ts:2587-2700`, `src/units/format.ts`, tests + `e2e/live-dim-*.spec.ts` | 4e | — | E3 | 300 | `bunx vitest run src/tools/sketch/liveDim* src/features/sketch`; the live-dim specs |
| 4.5 | Overlay collision opt-in (S9) | careful | `src/viewport/engine/SnapIndicator.ts:404-415`, `src/features/sketch/ConstraintBadgeLayer.tsx:157-161`, `SelectionDimensionLabels.tsx:66`, `src/viewport/engine/ViewportEngine.ts:2314-2340`, `annotationPlacement.test.ts` | 4f | 4.1, 4.2 | E1 | 200 | `bunx vitest run src/viewport/engine src/features/sketch`; `e2e/constraint-badge-standoff.spec.ts` |
| 5.1 | Screen-space edge pick (T8) | careful | `src/viewport/engine/Picker.ts:68-129, 444-502`, `Picker.test.ts`, new `e2e/edge-pick.spec.ts` | D9 | — | E4 | 220 | `bunx vitest run src/viewport/engine/Picker.test.ts`; new spec |
| 5.2 | Fillet from face; Hole/Measure/Combine gating (C6, C7) | careful | `src/tools/modelTools/toolApplicability.ts:113-189`, `ModelToolController.ts` (edge-op arm), `toolApplicability.test.ts`, `e2e/filletChamfer.spec.ts` | D9 | 5.1 | E4 | 200 | tests + spec |
| 5.3 | Commit boundary 4 (WP-4 + WP-5) | — | TODO.md | — | 4.*, 5.* | — | — | full L3 green |
| FP-* | FE polish (28 rows: V1–V5, T1–T7, N1–N12, C1–C11, S12–S14) | per row | see the FP table in the Design § WP-6/7/8 reference (Plan-agent table, verified) | D10–D14 | 5.3 | groups G-VP, G-MT, G-INS, G-CHR, G-COPY | ~2,700 total | per-row vitest/e2e/cargo commands; commit boundary 5 after full L3 |

FP table rows (id · group · est.): FP-V1 G-VP 60 · FP-T4 G-VP 40 · FP-V2 G-VP 200 · FP-V3 G-VP 90 · FP-V4 G-VP 130 · FP-V5 G-VP 110 · FP-T6 G-MT 120 · FP-T1 G-MT 140 · FP-T2/T3 G-MT 190 · FP-T7 G-MT 90 · FP-N9 (folded into 1.4) · FP-N3/N4/N11/T5 G-INS 230 · FP-N7/N8 G-INS 130 · FP-N10 G-INS 90 · FP-S12 G-INS 260 (D14) · FP-C1 G-CHR 25 · FP-C2 G-CHR 80 · FP-C3/C4/C5 G-CHR 90 · FP-C11 G-CHR 25 · FP-C9 G-CHR 45 · FP-C10 G-CHR 70 · FP-C6 (folded into 5.2) · FP-C8 G-CHR 120 (variant A) · FP-N1 G-COPY 90 (Rust) · FP-N2 G-COPY 70 · FP-N5 G-COPY 55 · FP-N6 G-COPY 55 · FP-N12 G-COPY 70 (Rust + mock) · FP-S13 G-COPY 35 · FP-S14 G-COPY 30. Each row's exact files, design notes, red-first test file and verify command are in the Plan-agent table (kept verbatim in the repo `PLAN.md` appendix at run start).

## Edge cases and failure modes

- **WP-1:** host face split into two coplanar faces (ladder tie ⇒ NeedsRepair, never a guess); face flipped orientation (transport keeps `n1·n0 > 0`; A1 rules the flipped case); face rotated about its normal (sketch stays world-aligned; the projected boundary goes stale — the existing WP-P banner must fire, test it); checkpoint restore base that swallows the host's producer (planner guard `suffix_sketches_stay_planned` already refuses); `Superseded` regen must discard the placement; a Sketch step with `hostFace` but no `primary` (evidence-only ref) resolves by descriptor only; legacy record with no `host_face` behaves exactly as today (frozen) — pinned by a byte-identical golden hash test.
- **WP-2:** refresh-only upsert on document open moves legacy geometry — one visible "Resolve sketch" entry, documented; write-back must not fire while a drag gesture is active (the lane claim already serialises); a solve that FAILS (`success=false`) writes nothing back and reports Conflicting with culprits; ellipse sketches (naive DOF path) never carry positions; drive-only drag of a point welded to `referenceLocked` geometry must refuse (pins at tag 0 still hold); `partial` status on non-convergence restores the pre-step pose (existing rule).
- **WP-3:** unfillet when the two legs are parallel or the intersection is farther than 10× the arc radius ⇒ plain delete + hint (no corner); trim of a circle into two arcs carries the Diameter to both; endpoint tangency on a zero-length leg refuses at authoring (`MIN_GEOMETRY_SIZE`).
- **WP-4:** the readout must keep showing the snapped point during the click frame; Alt suppresses snapping but the indicator must clear the same frame; the grid fix must not regress `sketch-degenerate.spec.ts` (four displacing sources disabled there on purpose).
- **WP-5:** a face selected for fillet whose edges are all non-manifold/seam ⇒ refuse with the existing edge-op refusal copy; body selection reason text must not enable the tool.
- **FP:** see the Plan-agent edge list (V1 badgeLayout prose, V2 tiny viewport fallback, V4 face planes keep their xAxis, V5 no clear mid-drag, T6 legacy records default Blind/NewBody, T2/T3 no double negation, T7 inch quantum, N7/N8 applying phase, N12 bound expressions, C8 spec changes, C6 hint suppression).

## Verification

- Per task: the `verify` column, run by the implementer AND re-run on the main thread before acceptance (a subagent's claim is not evidence).
- Checkpoint interval: `gate-verifier` runs L2 of the touched layer after every task group (B, C, D, E1–E4, each FP group).
- Commit boundaries (five): full L3 — worker build+stage · `ctest --test-dir worker/build` · `cargo fmt --all --check` · `cargo clippy --workspace --all-targets -- -D warnings` (1.97 + newest stable in a scratch target dir) · `ONECAD_REQUIRE_WORKER=1 cargo test --workspace --no-fail-fast` (check for skips) · `bunx tsc --noEmit` · `bun run build` · `bun run test` · hex grep · `verify-modeling-coverage.mjs`, `verify-modeling-contracts.mjs`, coverage self-test, stdout hygiene · `bun run e2e` both projects `retries: 0`, alone on the machine. Counts recorded, never "green".
- Protocol: `protocol-auditor` before and after every SCHEMA/fixture change (1.1, 2.1, 3.2); `canonical_*` ctest replays; worker fingerprint `0a6a1dce34181289` unchanged unless the auditor rules a bump.
- Astra: A1/A2/A3 accepted into `docs/design/astra/`; each `break` verdict + accepted/rejected findings in the Run log; a kernel/identity package is not finished until that entry exists.
- Whole-plan acceptance (real app, `onecad-agent-test` harness, cold start): the review's B1 flow (40 → face sketch → cut → edit 25) shows the pocket following; A-182 rectangle via Line tool reports Redundant/Under, never Conflicting; corner drag moves; fillet → finish → 1 region; trim keeps Ø; grid readout steps; badge names End/Mid; edge hover highlights and fillet arms from an edge and from a face; new project opens at FOV 35; error text is a sentence. Owed user-run: the manual checklist rows the harness cannot close (physical modifier drags), STEP/3MF open-elsewhere unchanged.

## Constraints

- Never edit `corpus/`, `src/test/contracts/*` (except D14, recorded), or `*.golden.test.*` except where a decision above names it; never rewrite `intent` subtrees; Rust stays the hash authority; `onecad-core` stays tauri-free; stdout hygiene; no raw hex; Z-up verbatim; no new dependencies; `settingsStore` version stays 11 unless a persisted key is added (then 12 + migration + `resetStores.ts`).
- Persisted sketch plane bases (`plane.rs:57-64`) never change (V4 is camera-only).
- Every diff orchestrator-reviewed; identity/regen/protocol/viewport diffs get a fresh-context `adversarial-reviewer`; gates re-run on the main thread; no gate under concurrent heavy load.
- Commits only at the five boundaries, on `master`, never push; never stage `.claude/agent-memory/**`, `src-tauri/.claude/`, `.tauri-agent/`.
- Effort: WP-1/2/3 implementation at xhigh (`impl-critical`), WP-4/5 careful (`impl-careful`), FP rows per tier; Astra xhigh, no `ultra`.

## Open questions

(none — the numerical unknowns are closed by A1/A2/A3 inside the run; the product decisions are D10–D14)

## Run log

(appended by fable-run)

## Follow-ups

- Datum planes following a moved host (`resolve_datum` unwired) — same design as WP-1.
- Re-projection of a host face's `referenceLocked` boundary on regen (WP-P banner covers staleness today).
- Session-32 intermittent extrude correlation timeout (needs its own repro).
- Per-entity constrained-state colouring is live; per-point DOF (Shapr3D 5.810) needs a solver channel.
- Expression input in live chips; Shift+letter constraint shortcuts already exist — document them in the constraint menu rows.
- S14 constraint-list order: verify after WP-3 (remap functions mint fresh ids); add stable per-row identity if it still reorders.
- Coincident badges default stays off (recorded decision v9→v10); revisit only with the per-point colouring.

## Appendix — FE polish task table (WP-6/7/8; Plan-agent draft, orchestrator-reviewed; decisions D10–D14 applied)

Lanes: G-VP (viewport files), G-MT (`ModelToolController.ts`), G-INS (inspector), G-CHR (chrome, file-disjoint), G-COPY (copy/IPC, file-disjoint). Rows inside G-VP/G-MT/G-INS are serialised by `deps` (same file).

| id | goal | tier | files | design notes | deps | group | est | verify |
|---|---|---|---|---|---|---|---|---|
| FP-V1 | Open every document at FOV 35°, same three-quarter iso framing | careful | `CameraRig.ts:48-50`, `ViewportEngine.ts:277`, `CadOrbitControls.ts:239-241,628-634`, `viewportStore.ts:261`, `src/test/resetStores.ts:43` | One exported `DEFAULT_FOV_DEG = 35`; no-bounds default distance 260 → 644 (= 260·tan38°/tan17.5°) so `orthoHalfHeight` is invariant; ISO yaw/pitch unchanged; fix the fov-76 prose in `badgeLayout.ts:123`/its test | — | G-VP | 60 | new `cameraDefaults.test.ts` (fov 35; ortho half-height within 1 %) + `CameraRig.test.ts cameraFit.test.ts StatusBar.test.tsx` |
| FP-T4 | Cut preview tints only the tool volume | standard | `ViewportEngine.ts:1898-1906` | keep `previewMesh.setTint(cut)`/`revolvePreview.setTint(cut)`, drop the `previewMaterials.setFaceColor(destructive)` branch, always `resetFaceColor()` | FP-V1 | G-VP | 40 | new `previewTint.test.ts` + `e2e/boolean-preview.spec.ts` |
| FP-V2 | `⇧F` centres the model with margin | careful | `viewportWorkAreaStore.ts:63-105`, `cameraFit.ts:35-82`, `ViewportEngine.ts:1230-1243`, `ViewportRoot.tsx:263` | add `fitSafeRect` = symmetric inset of `available` (per-edge obstacle insets, then left=right=max, top=bottom=max) beside `obstacleClearRect` (overlay placement untouched); `setFitSafeRect()` feeds `fitBounds` only; tiny viewport ⇒ fall back to `available`; assert bounds walk `bodiesRoot` only | FP-T4 | G-VP | 200 | new `fitSafeRect.test.ts` (asymmetric obstacles ⇒ target within 1 px of centre) + `viewportWorkAreaStore.test.ts cameraFit.test.ts` |
| FP-V3 | Face-sketch entry pads and the tint stops hiding the body | standard | `ViewportEngine.ts:1219-1227,1393-1449`, `SketchObject.ts:352-365` | `SKETCH_ENTRY_PADDING = 1.6` on `enterSketch` bounds via `fitSafeRect`; tint quad opacity 0.5 → 0.22 (material only) | FP-V2 | G-VP | 90 | new `sketchEntryFraming.test.ts` + `e2e/sketch-on-face.spec.ts` |
| FP-V4 | TOP of a world plane shows +X right / +Y up; readout labels X/Y there | careful | `ViewportEngine.ts:1442`, `viewportStore.ts:452`, `StatusBar.tsx:155-162` | pass `xAxis` to `orientationAlongNormal` only for face/datum planes; world planes use `yawForDirection`; `formatCursorPlane` gains a label pair; persisted bases untouched (assert) | FP-V3 | G-VP | 130 | new `planeOrientation.test.ts` + `StatusBar.test.tsx` + `e2e/datum-sketch.spec.ts` |
| FP-V5 | Readout blanks off-canvas and over chrome | standard | `ViewportRoot.tsx:660-717`, `viewportStore.ts:198,293-295` | `setCursor(null,null)` legal; `pointerleave` + early return when `isInteractiveBoundary(e)`; no clear mid-drag; StatusBar renders `—` | FP-V1, FP-V2 | G-VP | 110 | new `cursorReadoutBlank.test.tsx` + `ViewportRoot.test.tsx StatusBar.test.tsx` |
| FP-T6 | Re-edit exposes end condition, symmetric, boolean mode | careful | `ModelToolController.ts:1755-1760,1640-1660` | flags true on re-edit, seeded from `extrudeStoredParams` (`storedScalar` pattern); legacy records default Blind/NewBody; `canUseBodyEnds` recomputed for the current doc; NewBody↔Add/Cut change hints "downstream features may need repair" | — | G-MT | 120 | new `ModelToolController.reeditOptions.test.ts` + `commit.test.ts ModelToolChips.test.tsx` |
| FP-T1 | Chip beside the profile, swallowed click reported | careful | `ModelToolController.ts:1646-1647`, `toolChipPlacementStore.ts:16-19` | anchor offset along in-plane +x by the profile half-extent; pointerdown absorbed while a region is required ⇒ one-shot hint | FP-T6 | G-MT | 140 | new `chipAnchor.test.ts` + `e2e/extrude-commit-gesture.spec.ts` |
| FP-T2/T3 | Visible "Symmetric" label + app Tooltip; "Flip" direction control | careful | `ExtrudeChipControls.tsx:144-161`, `toolChipStore.ts`, `ModelToolController.ts`, `ActiveToolInspector.tsx:130-152` | label text next to the glyph, `src/ui/Tooltip`; `DirectionFlipButton` "Flip" (never "cancel"/"✕" — chip probe) → `onFlip` → `resolveDepth(raw,{flip})` (`depthProjection.ts:56-58`); no double negation with drag-through-zero; arrow and number agree | FP-T1 | G-MT | 190 | new `ExtrudeChipControls.test.tsx` + `modelingInteraction.golden.test.tsx ModelToolChips.test.tsx` (golden must stay green untouched) |
| FP-T7 | Depth drag rounds like sketch dimensions | standard | `ModelToolController.ts:7930-7940,8125-8135`, `depthProjection.ts:62-66` | first call site for `snapDepth` with `dimQuantum(chooseGridStep(engine.getCameraDistance()).minor, displayUnit).length` when `snapTo.dimensionRound`; round in mm after `displayToMm`; typed values exact; no new setting | FP-T2/T3 | G-MT | 90 | new `depthRounding.test.ts` + `e2e/live-dim-mouse-rounding.spec.ts` |
| FP-N9 | (folded into WP-1 task 1.4) | | | | | | | |
| FP-N3/N4/N11/T5 | Targets block names things; Direction live | careful | `activeToolPresentation.ts:104-120,262-300,343`, `ActiveToolTargets.tsx:22-33,90-99,140-155,166` | title from `state.edgeOp` ("Fillet"/"Chamfer"); slots "First face"/"Second face", empty "Awaiting pick"; face label adds short id + area/normal when carried; Direction from signed value + symmetric (+Normal / −Normal / Both) | — | G-INS | 230 | `activeToolPresentation.test.ts` + new `ActiveToolTargets.naming.test.tsx` |
| FP-N7/N8 | Completed recap clears on selection change | careful | `ActiveToolInspector.tsx:250-298`, `InspectorPanel.tsx:185-222` | clear `operationAttemptStore` on the first selection write after `phase === "completed"`; `applying`/`failed` keep priority; not during a re-edit's applying phase | FP-T2/T3 | G-INS | 130 | new `ActiveToolInspector.recap.test.tsx` + `InspectorPanel.test.tsx InspectorPanel.golden.test.tsx` |
| FP-N10 | Consumed sketch never reads "Not evaluated" | standard | `InspectorPanel.tsx:288-300`, `constraintStatus.ts:15-25` | "Consumed by <feature>" (tone ok) when an applied feature depends on the sketch; pure helper in `constraintStatus.ts` | FP-N7/N8 | G-INS | 90 | `constraintStatus.test.ts` + `InspectorPanel.test.tsx` |
| FP-S12 | Selected sketch entity shows properties | careful | `InspectorPanel.tsx:192-221`, new `SketchEntityProperties.tsx`, `sketchSelectionStore.ts:34-51` | section above `SketchState` (type, endpoints/centre/radius, referencing constraints via ConstraintList rows); **D14: `INSPECTOR_SECTIONS_CONTRACT.sketchMode` gains "Entity" — recorded decision, contract + golden updated with the decision cited** | FP-N10 | G-INS | 260 | new `SketchEntityProperties.test.tsx` + `InspectorPanel.golden.test.tsx` |
| FP-C1 | Escape closes the snap popover | standard | `SnapPopover.tsx:104-113` | `autoFocus="first"` as `NavigationHelp.tsx:22` | — | G-CHR | 25 | new case in `SnapPopover.test.tsx` |
| FP-C2 | Tall popover stays in the work area and scrolls | careful | `Popover.tsx:60-80,228-240` | clamp against the chrome-inset area (status bar 34 px `BOTTOM_INSET`), `maxHeight` clamped, `overflowY:auto` | — | G-CHR | 80 | new case in `Popover.test.tsx` |
| FP-C3/C4/C5 | Tooltips delay 450 ms, hide on click/Escape, above popovers | standard | `Tooltip.tsx:17-59` | open timer; document `pointerdown` + Escape hide; `z-[110]` | — | G-CHR | 90 | `Tooltip.test.tsx` new cases + `ToolButton.test.tsx FloatingToolbar.test.tsx` |
| FP-C11 | No "Library ()" | standard | `ToolButton.tsx:61` | `shortcut ? \`${label} (${shortcut})\` : label`; no shortcut minted (keeps keymap/toolbar contracts untouched) | — | G-CHR | 25 | `ToolButton.test.tsx` new case |
| FP-C9 | `⌘S` on a never-saved doc goes straight to Save As | standard | `fileActions.ts:83-95` | check the known-path fact in `documentStore` first; `isNoPathError` stays as backstop | — | G-CHR | 45 | `fileActions.test.ts` new case + `e2e/unsaved-guard.spec.ts` |
| FP-C10 | History rows do not shift on select | standard | `HistoryList.tsx:308-320,360-372,450-479` | action cluster as `absolute inset-y-0 right-2` overlay; edit row only while editing | — | G-CHR | 70 | `HistoryList.test.tsx` new case + `e2e/history-rollback.spec.ts` |
| FP-C6 | (folded into WP-5 task 5.2) | | | | | | | |
| FP-C8 | Visibility toggles off the undo stack (D11, variant A) | careful | `treeActions.ts:134-155`, `src-tauri/src/document_runtime.rs` (undo classification), `edit/undo.rs` | `SetVisibility` records no undo entry (view-state transaction, still persisted); `e2e/model-undo.spec.ts` + `tree-visibility.spec.ts` expectations change with the recorded decision | — | G-CHR | 120 | `treeActions.test.ts` + both specs |
| FP-N1 | Kernel failures read as sentences | careful | `onecad-core/src/regen/engine.rs:585-604`, `src-tauri/src/error.rs:65-93,128-170` | `OpFailed` Display = message only; code/recoverable stay structured (tracing fields + diagnostics); tests pinning the old string update | — | G-COPY | 90 | `cargo test -p onecad-core` (new Display test) + clippy |
| FP-N2 | Status bar one line; no kind prefix | standard | `tauriClient.ts:429-443`, `StatusBar.tsx:83-104` | `toClientError` stops prefixing `${kind}: ` (`err.kind` stays); hint `truncate max-w-[42ch]` + full text in `title` | FP-N1, FP-V4 | G-COPY | 70 | `tauriClient.test.ts StatusBar.test.tsx` new case (200-char hint keeps `fov`/`dof` on one row) |
| FP-N5 | Measure shows the body name | standard | `measurePresentation.ts:9-12`, `MeasurePanel.tsx` | `bodyLabel(id, names)` from `documentStore.bodies`, id fallback only when the body is gone | — | G-COPY | 55 | `measurePresentation.test.ts` + `e2e/measure.spec.ts` |
| FP-N6 | Palette shows human categories | standard | `paletteItems.ts:43-48` | `GROUP_LABELS` map; unmapped dotted id drops the suffix | — | G-COPY | 55 | `paletteItems.test.ts` (no rendered category contains ".") |
| FP-N12 | Negative feature value keeps its sign | careful | `dto.rs:2175-2190`, `mockClient.ts:1346-1359,2380` | drop `.abs()` on text and primary; inline editor round-trips the sign; bound expressions unchanged | — | G-COPY | 70 | `cargo test -p onecad feature_value` (new `-15.0 mm` case; staged worker) + `HistoryList.test.tsx featureValueEdit.test.ts` |
| FP-S13 | Re-opening a sketch lands in Select | standard | `toolStore.ts:137-145` | `setMode("sketch")` without `opts.tool` ⇒ `"select"`, phase idle; `activateTool.ts:130` keeps its explicit tool | — | G-COPY | 35 | `toolStore.test.ts` new case + `e2e/sketch-reattach.spec.ts` |
| FP-S14 | Sketch Select has a status hint | standard | `SketchController.ts:1720-1730` | `select` entry ("Click geometry to select · drag to move · Delete to remove"), ungated by `liveDimsEnabled()` | — | G-COPY | 30 | new `SketchController.hints.test.ts` |

Contracts: only D14 (`INSPECTOR_SECTIONS_CONTRACT.sketchMode` + `InspectorPanel.golden.test.tsx`) may change, with the decision cited in the test; `modelingInteractionContract` / `toolbarContract` / `keymapContract` untouched. `settingsStore` stays at version 11 (no persisted key added).
