GOAL: A sketch attached to a solid face follows that face through regeneration (B1), an unresolvable host raises a deterministic NeedsRepair and halts dependents, and a Cut that removes no material is refused — across SCHEMA, worker, Rust and tests.

CONTEXT: repo /Users/andrejvysny/workspace/OneCAD. Read first: CLAUDE.md (§ Invariants, § C++ worker, § Rust, § Testing), `PLAN.md` § Context "B1", § Decisions D1/D2, § "Plan-review amendments" A-1/A-2/A-3/A-9/A-10/A-14, § "Protocol pre-audit and Astra derive outcomes" P-1..P-5, § Design WP-1. The accepted derivation you IMPLEMENT (do not redesign) is `docs/design/astra/sketch-host-face-transport.md` — sections 3 (transport, epsilons, planarity, seat witness, Cut predicate), 4 (degeneracy table), 5 (algorithm), 6 (test vectors), 8 (handoff interfaces + diagnostic codes).
Precedent to mirror line by line: the component mate reseat — worker `worker/src/ops/ComponentOp.cpp:286-360` (`resolve_mate_reseat`: `em::ladder_ref_from_input`, tracked rung `topokey_for_element_in_body` → `shape_for_topokey`, then `resolve_descriptor_stage` with `LadderEditContext{post_upstream_edit, from_zero_replay}`, repair items via `mate_unresolved_repair` / `mate_op_built_item`, `BRepClass_FaceClassifier` seat check :501-540), SCHEMA §7.2 `matePlacement` (:698-710) and §7.3 PlaceComponent "Re-seated by the worker" (:2760-2800), Rust `onecad-core/src/regen/executor.rs:259-312` (`mate_placement_by_step` collection) and `src-tauri/src/document_runtime.rs:2229-2237, 2600-2660` (`sync_mate_placements`, inside the fencing guard), `src-tauri/src/worker/wire.rs:906-917` (`parse_mate_placement`).
Sketch step today: `worker/src/session/PlanExecutor.cpp:420-434` (`run_single_op` Sketch branch materialises raw params into `job.sketches`; ops find the sketch via `ExtrudeOp.cpp:137-145 find_sketch`; the plane is parsed by `WireSketch::parse_plane`). The generic `inputs[]` pre-flight (`PlanExecutor.cpp:709-714`) skips `run_single_op` when it left repair items — so `hostFace` must NOT be in `inputs[]`; your Sketch branch resolves it itself and returns repair items through its own `ops::OpOutcome` (check how an op returns needs-repair items — e.g. `execute_place_component` — and how `:560` maps a body-less step with items to `NeedsRepair`, which halts the plan at m−1; that halt is the intended behaviour, A-1).
Rust side today: `onecad-core/src/sketch/mod.rs:77-102` (`SketchAttachment::HostFace{face: ElementRef, projected_boundary_version}`), `:303-337` (`Sketch.plane` authored frame), `:416-430` (`host_face()`); `onecad-core/src/document/record.rs:1252-1284` (`SketchOpParams.host_face`, wire-omitted today) and `:343-352` (`element_refs_mut` has NO Sketch arm — add one returning `host_face` as its only entry, A-2/P-1); `src-tauri/src/worker/wire.rs:449-468 strip_sketch_host_face` (remove the strip; invert the golden `wire.rs:5084 sketch_host_face_is_dropped_from_the_wire_params` and the `("Sketch", …, 0, 0)` slot row at `wire.rs:7983` deliberately — P-3); `document_runtime.rs:1408 hydrate_ref_intents/stamp_intents` (mint/re-edit stamping, never on load); `onecad-core/src/document/repair.rs` (`RepairReason` closed list + `Unknown` fallback, round-trip test :642-661); Cut tails `worker/src/ops/ExtrudeOp.cpp` (~:1287 before `publish_boolean_result`; the Add-disjoint refusal lives there), `RevolveOp.cpp`, `BooleanOp.cpp` (`stage:"publish"` precedent :112). Volume: `BRepGProp::VolumeProperties(shape, props, Eps)` RETURNS the achieved relative error — use it for U. Precision: `worker/src/kernel/validation/GeometryPrecision.h` (`precision_of(shape).authoring_resolution()`, volume floor = a³).
Tests to extend: `src-tauri/tests/sketch_on_face.rs` (real worker, `real_worker()` pattern), `worker/tests/CMakeLists.txt` (`canonical_*` block ~:1104-1118 is hand-registered), `protocol/fixtures/place_component_mate_reseat.ndjson` (fixture shape precedent), `worker/tests/test_component_mate_reseat.cpp` (worker test precedent), `worker/tests/test_revolve_boolean_modes.cpp:203` ("revolve-cut-miss: volume unchanged … removes nothing" — this assertion LEGITIMATELY FLIPS to expect the refusal, decision A-3).

DESIGN (already decided — implement exactly):
1. SCHEMA (`protocol/SCHEMA.md`), all additive, one §14 entry per the pre-audit skeleton in PLAN.md P-2/P-3 and the auditor text (in `PLAN.md` § Protocol pre-audit): §7.3 Sketch params OPTIONAL `hostFace` (same typed semantic-ref shape as Extrude `targetFace`; NOT in `inputs[]`, the `mate.target` rule) + OPTIONAL `frameTransportVersion: 1`; §7.2 `planStep.sketchPlacement: { plane:{origin,xAxis,yAxis,normal}, translationMm, rotationDeg }` present iff the host resolved AND the effective frame ≠ authored (exact `moved`, no deadband); `diagnostics` info `SKETCH_HOST_RESEATED` (stage `"sketch"`, evidence `{translationMm, rotationDeg, significant}`) and `SKETCH_HOST_NORMAL_REVERSED`; §9 op-built reasons `sketchHostNonPlanar`, `sketchSeatOffFace`, `sketchFrameIllConditioned`, `sketchFrameInvalid`, `sketchSeatUnmeasurable`, `refId "<opId>.input0"`; §7.3 Boolean/Extrude/Revolve prose (NOT the §8 table): `OP_FAILED` reasonCode `CUT_NO_EFFECT` / `CUT_VOLUME_INCREASED` / `CUT_VOLUME_UNMEASURABLE`, `stage:"publish"`, evidence `{volumeBefore, volumeAfter, toleranceMm3, relErrBefore, relErrAfter}`, carried on `perStepResults[].diagnostics` + `error.detail.diagnostics`. New fixture `protocol/fixtures/sketch_host_face_reseat.ndjson` (rounds: G1 follow 40→25 with `sketchPlacement` + info; host vanished ⇒ `needsRepair` item at `<opId>.input0` and the plan halts at m−1; non-planar host ⇒ `sketchHostNonPlanar`; imprint-only Cut ⇒ `CUT_NO_EFFECT` refusal) registered as a `canonical_*` ctest. Fingerprint `0a6a1dce34181289` must not move (kernelPolicyVersion stays 1).
2. Worker: new `worker/src/session/SketchHostReseat.{h,cpp}` implementing A1 §8: `transport_sketch_frame(frozen, plane, policy)` (o1 = o0 + ((p1−o0)·n1) n1; qx = x0 − (x0·n1)n1, h = |qx|; h ≤ τGS = sqrt(DBL_EPSILON) ⇒ ill-conditioned repair (compute the y-fallback only as repair evidence); x1 = qx/h; y1 = n1 × x1; n1 orientation-corrected from the face (FORWARD/REVERSED); when n1 == n0 exactly carry x0/y0 verbatim), `classify_sketch_seat(face, frozen, candidate, frozen_anchor)` (anchor → (ua,va) in F0 → anchor1 in F1 → `BRepClass_FaceClassifier` at `BRep_Tool::Tolerance(face)`; OUT ⇒ `sketchSeatOffFace`, UNKNOWN/exception ⇒ `sketchSeatUnmeasurable`; evaluated even when the plane is unchanged), frame validity predicate (finite, unit, orthogonal, x×y·n > 0 ⇒ else `sketchFrameInvalid`), planarity = `BRepAdaptor_Surface::GetType() == GeomAbs_Plane` (no distance epsilon), `FrameChange{moved, significant, translation_mm, rotation_rad}` with θframe from Q = Bc·B0ᵀ, significance thresholds 1e-3 mm / 0.01°. Host resolution in the Sketch branch: tracked rung first, then `resolve_descriptor_stage`; accept ONLY AutoBind with score ≥ 0.85 AND margin ≥ 0.10 (strict gate, A1 §1 — Ladder.cpp:342 admits anchor-decided binds below margin; refuse those as `ambiguous`), else the ladder's own repair json. On success rewrite the materialised `params.plane` to the effective frame (local entity coordinates untouched) and stash `sketchPlacement` + diagnostics for that step's `planStep`. Cut: in every subtracting tail before `publish_boolean_result`: `VolumeProperties(target, Eps=1e-6)` before/after, U = errB·VB + errA·VA, εV = a³ + U; refuse per P-5.
3. Rust: wire lowering sends `hostFace` (+ `frameTransportVersion`); `parse_plan_step` gains `sketch_placement`; `executor.rs` collects `sketch_placement_by_step`; `document_runtime.rs::sync_sketch_placements(executed)` writes DERIVED `Sketch.resolved_plane: Option<SketchPlane>` (new field on `Sketch`, serde `skip_serializing_if = None`, NOT part of `sketch_geometry_token` or any hash; NOT the record's `plane`, which stays the authored frame — A-9/P-4) inside the same fencing guard as `sync_mate_placements`, and clears it when a regen reports the host resolved with `moved == false`; `Superseded` discards. The projection publishes the EFFECTIVE plane (resolved_plane or plane) — find where `SketchDto.plane` is built (:3617 neighbourhood `sketch_host_face`) and use the effective frame; `enter_sketch`/`finish_sketch` sketch wire uses the effective frame for the solver lane too (the sketch is edited where it is displayed), while the timeline record keeps the authored frame. `element_refs_mut` Sketch arm; `RepairReason` variants; `parse_input_ref_id` unchanged.
4. Tests (red-first, each proven red before the fix): Rust `sketch_on_face.rs`: (a) box 100×60×40 → sketch on top → rectangle 30×20 + Ø25 circle → extrude-cut 15 → edit host 40→25 ⇒ projection plane origin z = 25 (1e-9), pocket volume = the kernel-exact expected (compute from the mass properties in the test, cite the numbers), diagnostics carry `SKETCH_HOST_RESEATED`, timeline record plane STILL z = 40, undo ⇒ resolved_plane cleared, z = 40; (b) delete/replace the host so the face vanishes ⇒ `NeedsRepair` `refId <sketchOpId>.input0`, sketch still in the document at z = 40, plan halted (downstream cut not published); (c) imprint-only cut (tool starts above the material and lands exactly on the face) ⇒ `OpFailed` with `CUT_NO_EFFECT`; (d) legacy record without `host_face` ⇒ byte-identical behaviour (frozen). Worker `worker/tests/test_sketch_host_reseat.cpp`: A1 §6 vectors G1/G2/G4/G8 numeric (1e-9), τGS boundary (b = τGS/2 repairs, 2τGS transports), invalid handedness, significance thresholds (0.000999/0.001/0.001001 mm; 0.009999°/0.01°/0.010001°), annulus origin-OUT-anchor-IN accept, seat OUT repair; Cut G10/G11/G12 with computed εV. `cargo test -p onecad-core --test regen_planner` goldens byte-identical (A-14). `test_revolve_boolean_modes.cpp:203` flipped to expect the refusal (say so in the test comment, cite decision A-3).

CONSTRAINTS: only ONE agent builds the worker on this machine — you are it for this package; run `ONECAD_OCCT_ROOT=$HOME/.onecad-occt/8.0.1 scripts/build-worker.sh Release` before any cargo command (stages the sidecar). Do not touch `protocol/mesh_format.md`, `corpus/`, `src/` (frontend — a separate task), `src/test/contracts`, goldens other than the two named inversions. Keep `onecad-core` tauri-free. C++20, `-Wall -Wextra`, logs via `WLOG_*` to stderr only. Never rewrite `intent` subtrees; never backfill `host_face` on load. Rust clippy `-D warnings` clean, `cargo fmt`. No feature flags. Keep the Sketch step's raw-params behaviour byte-identical when `hostFace` is absent.

VERIFY (run yourself, report the measured counts): `ctest --test-dir worker/build -R 'sketch_host|canonical_sketch|extrude|boolean|revolve|component_mate' --output-on-failure`, then the FULL `ctest --test-dir worker/build`; from `src-tauri/`: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `ONECAD_WORKER_PATH=$PWD/../worker/build/onecad-worker ONECAD_REQUIRE_WORKER=1 cargo test --test sketch_on_face --test wire_contract --test topology_rebind --test component_ops`, `cargo test -p onecad-core --test regen_planner --test edit_session`, `cargo test -p onecad-protocol`; `scripts/check-worker-stdout-hygiene.sh`. Expected: all green, fingerprint unchanged (`grep fingerprint src-tauri/binaries/onecad-worker-manifest.json`).

REPORT: files changed with line counts; the exact §14 entry text; the fixture rounds and their measured outputs; each red-first test's failing assertion before the fix; every verify command's summary line; any deviation from this brief and why; anything you could not verify.
