GOAL: The sketch solver tells the truth (B2, B6): `SketchUpsert` reports and Rust adopts the solved geometry on every upsert site, a converged-but-violating solve is classed `Conflicting` with named culprits through a per-dimension residual gate, and a point drag drives only the dragged point — across SCHEMA §7.4, worker, Rust and tests.

CONTEXT: repo /Users/andrejvysny/workspace/OneCAD. Read CLAUDE.md (§ Invariants, § C++ worker, § Rust, § Testing), `PLAN.md` § Context "B2", "B6", § Decisions D3/D4/D5, § amendments A-4/A-5/A-6/A-7/A-8/A-11/A-12, § "Protocol pre-audit and Astra derive outcomes" P-6/P-7, § Design WP-2. The accepted derivation you IMPLEMENT is `docs/design/astra/sketch-solver-truth.md` — §3 (typed residuals, state table, culprits, drive-only drag, T9 branch guard), §4 (degeneracy table), §5 (algorithms), §6 (test vectors T2–T9 + boundary fixtures), §8 (interfaces, §7.4 changes). The FE half (direction-lock dedupe, row naming, vocabulary, hint copy, mock parity) is a SEPARATE task — do not touch `src/`.
Worker anchors: `worker/src/protocol/SolverLane.cpp:415-457 on_upsert` (solve, `upsert_state` :288-309, `apply_solved_positions(stored_args…)` :433-435 — the store already holds the solved pose; the RESULT does not report it), `:568-642 on_begin` (diagnosis before `beginPointDrag`; the comment at :568-577 explains the pin rationale — update it, do not delete), `:649-676` (per-kind pin sets in `run_step` — ArcEnd/Radius/EntityBody stay as they are, A-12), `:750-813 on_drag`, `:817-897 on_end` (final target step with pins active, THEN `endPointDrag`; no released solve — this is the B6 "conflict persists" mechanism), `changed_positions`/`changed_curves` helpers (reuse for the upsert result, baselined at the REQUEST's coordinates). `worker/src/sketch/Sketch.cpp:1310-1386 beginPointDrag/endPointDrag` (rectangle strategy + pin-everything fallback; keep `dragStartPositions_` rollback), `:1268-1308 solve()`. `worker/src/sketch/solver/ConstraintSolver.cpp:434` (`success = Success || Converged` — the conflation; keep `success` semantics for the drag lane but the GATE decides the public state), `:778-839 solveWithDragSingleStep` (drives via `addConstraintCoordinateX/Y(..., dragTag=-1, driving=true)`, cleared per step), `:1031-1045 diagnose/hasConflicting/hasRedundant`, `:1790-1792` (convergence AND convergenceRedundant = config tolerance 1e-4 — a squared-step criterion, NOT a residual bound). `worker/src/sketch/SketchConstraint.h:213-225 maxConstraintResidual` (skips unreadable ⇒ 0.0: the gate must NOT be built on it; enumerate covered kinds, report `unassessed`). PlaneGCS `GCS.cpp:5600-5641 diagnose` (redundant iff err² < convergenceRedundant at the solved subset, else conflict group).
Rust anchors: `src-tauri/src/document_runtime.rs:4182-4224 sketch_upsert_with_outcome` (identity solve today), `:4354-4386 end_gesture_with_outcome` (the write-back precedent: `after.apply_solved_positions/curves` + `EditCommand::SketchDragGesture{before, after}`), the FIVE `solver.sketch_upsert` sites `:389` (PreparedSketchRegions::drive on a clone — NO adoption), `:4055` (enter_sketch — ADOPT, and move the solve AFTER `undo_watermark` is taken at :4061 so `squash_sketch_session` :5700-5710 collapses it), `:4221` (ADOPT, coalesced into the edit's undo entry, ONE `CommandOutcome` — `src-tauri/src/api/mod.rs:2001-2016` forwards exactly one outcome to the regen scheduler), `:4254` (begin_gesture — NO adoption), `:4535` (finish_sketch_with_outcome — ADOPT before `upsert_sketch_record` mints the timeline record; this is where B2's skewed prism is fixed). `src-tauri/src/worker/wire.rs:2419-2455 parse_sketch_upsert` (already parses `positions`/`curves` when present; add `reasons`/`unassessed`), `onecad-core/src/sketch/mod.rs:583 apply_solved_positions`, `:614 apply_solved_curves`, `onecad-core/src/edit/session.rs` (`EditCommand::SketchEdit` :766, `SketchDragGesture`, undo coalescing — add `apply_coalesced` or equivalent; one undo entry is the contract), `src-tauri/src/dto.rs:907-932 SketchUpsertDto`, `src-tauri/src/worker/manifest.rs:88,158 solverPolicyVersion` (1 → 2, and `worker/src/main.cpp:106` hello).
Tests to extend: `worker/tests/test_solver_residual.cpp`, `test_sketch_drag_kinds.cpp` (must NOT change — kinds keep their pins), `worker/tests/fixtures/sketch_*.ndjson`, `protocol/fixtures/sketch_solve_residual.ndjson` (header rewrite + ONE new round: converged-with-large-residual ⇒ `Conflicting`, `reasons`, culprits — A-8/P-7; existing rounds keep verdicts), NEW `protocol/fixtures/sketch_upsert_writeback.ndjson` (1°-off line + Horizontal ⇒ `positions` present, both endpoints on one y; the A-182-shaped rectangle (4 Coincident + 2 H + 2 V + 2 Perpendicular on ±0.8° authored geometry) ⇒ after write-back `OverConstrained`, never `Conflicting`), both registered as `canonical_*` in `worker/tests/CMakeLists.txt` (~:1104-1118); Rust `src-tauri/tests/sketch_edit.rs`, `sketch_conflicts.rs`, `sketch_direct_manipulation.rs` (drag of a welded rectangle corner moves; released state honest; `e2e/sketch-drag.spec.ts` may legitimately change — record it), `src-tauri/crates/onecad-core/tests/edit_session.rs` (coalesced undo), `regen_planner` goldens byte-identical.

DESIGN (decided — implement exactly):
1. SCHEMA §7.4 (+ §14 entry per the auditor skeleton in `PLAN.md` § Protocol pre-audit "WP-2"): `SketchUpsert` result gains OPTIONAL `positions` and `curves` (EndGesture shapes, delta vs the request's coordinates), OPTIONAL `reasons: { "<constraintId>": "residualViolation" | "rankConflict" }` and `unassessed: [ids]`; the `maxResidual` paragraph is rewritten: it stays as legacy mixed-dimension reporting, and the GATE is normative: τ_len = 1e-4 mm absolute; τ_ang(c) = τ_len / L_c with L_c the LOCAL operand span (one line: its length; two lines: the longer; other angular operands: documented per kind); a constraint is violated iff q_c = r_c/τ > 1 (`≤` accepts); `state` = `Conflicting` when the rank-conflict set is non-empty (reason `rankConflict`) OR any readable constraint is violated (reason `residualViolation`); `conflicting[]` = violated ids ∪ conflict-group members (tags 0/−1 never); unreadable/non-finite ⇒ `unassessed[]` and never "satisfied"; otherwise `OverConstrained` iff benign redundancy (all its residuals pass) else DOF classification. `EndGesture` performs an UNCONDITIONAL released exact solve + diagnosis + gate after removing the drives and reports fresh `state`/`dof`/`conflicting`/`reasons` (gesture-fixed `entityStates` still echoed; `status` precedence unchanged). `SolveDrag`: "cursor distance alone is never `partial`"; `partial` = solver failure / guard refusal with the pre-step pose restored; new `partial` detail `branchBoundary`. `solverPolicyVersion` 1 → 2 (hello, manifest test rows, §7.2 policyVersions prose).
2. Worker: (a) `on_upsert`: after the solve, run the residual gate (new `worker/src/sketch/ResidualGate.{h,cpp}`: `checkResiduals(constraints, sketch, τ_len, τ_ang) → {violations, unassessed}` enumerating the constraint kinds it can read — H/V are coordinate residuals in mm; Distance/H-/V-Distance/Radius/Diameter mm; Parallel/Perpendicular/Angle/Tangent angular with the local L_c; Coincident/Midpoint/Concentric/OnCurve/Fixed/Equal mm; Symmetric per its lowering; anything else `unassessed`) and `classifyExact(residuals, diagnosis) → {state, conflicting, reasons}`; emit `positions`/`curves` from the solved sketch vs the request (reuse `changed_positions`/`changed_curves` with the request pose as baseline); store the solved args as today. (b) `beginPointDrag`: remove both pin strategies; keep the rollback snapshot; `activeDragFixedPoints_` empty for Point drags; tag-0 locks untouched; the T9 branch guard for Angle-bearing sketches (signed area of the ordered rays, `sign(S0)·S > τ_len·L`, deterministic subdivision, else restore + `partial: branchBoundary`) — implement in `solveWithDrag`'s stepping loop. (c) `on_end`: final target step → `endPointDrag()` → `sketch->solve()` (exact, released) → diagnose → gate → report from THAT; positions/curves delta vs the gesture baseline as today. Update the `on_begin` comment. Add a ctest that pins "adding a constraint kind without a residual reader fails" (a static table of kinds vs `checkResiduals` coverage).
3. Rust: `parse_sketch_upsert` reads `reasons`/`unassessed` into `SketchUpsertDto` (additive fields, camelCase in dto.rs + `src/ipc/types.ts` lockstep is the FE task's — add the DTO fields here and leave the TS mirror to the FE task; note it in the report). Adoption per A-4 at the three ADOPT sites: build `after` from the current sketch, `apply_solved_positions` + `apply_solved_curves`, commit as `SketchDragGesture{before, after}` COALESCED into the triggering edit's undo entry (one entry, one `CommandOutcome`); `enter_sketch` reorders the solve after the watermark; `record_solve` unchanged. A solve with `success == false` writes nothing back. Never adopt while `active_gesture` is set.
4. Tests (red-first): worker `test_solver_residual.cpp` new cases: T2 (exact rectangle + 2 Perp ⇒ OverConstrained, conflicting []), the converged-with-large-residual case (author the A-182 rectangle with ±0.8°/−1.0° sides, request a solve that PlaneGCS reports Converged while a coordinate residual > 1e-4 remains — if PlaneGCS instead solves it exactly, construct the local-minimum case Astra names or a Distance/Angle pair that converges to a wrong root; document which), T3 unchanged, boundary residuals 0.00009999/0.0001/0.00010001 mm (pass/pass/fail), an unreadable constraint ⇒ `unassessed` not culprit; `test_sketch_drag_kinds.cpp` untouched; new `test_sketch_point_drag.cpp`: T5 (welded 80×60 rectangle, H/V only, drag A to (22,21) ⇒ A=(22,21), B=(80,21), D=(22,60), C unchanged, status success, released state UnderConstrained DOF 4, conflicting []), T6 (no constraints ⇒ only the dragged point moves), T7 (locked reference ⇒ locked scalars byte-identical), T9 branch guard. Rust: `sketch_edit.rs` (an upsert moves the DOCUMENT's coordinates; one undo entry restores both edit and solve; the outcome reaches the scheduler), `sketch_conflicts.rs` (A-182 set ⇒ OverConstrained on solved geometry), `sketch_direct_manipulation.rs` (corner drag then honest released state), `edit_session.rs` (coalesce), `regen_planner` goldens byte-identical.

CONSTRAINTS: worker builds are serialised — coordinate with the orchestrator before `scripts/build-worker.sh` (run it yourself only when told the tree is yours; assume it is when this brief is sent). `ONECAD_OCCT_ROOT=$HOME/.onecad-occt/8.0.1 scripts/build-worker.sh Release` before any cargo command. Do not edit `src/` (frontend), `corpus/`, `mesh_format.md`, `test_sketch_drag_kinds.cpp`, goldens. C++20, `-Wall -Wextra`, `WLOG_*` to stderr, stdout frames only. `onecad-core` tauri-free. Clippy `-D warnings`, `cargo fmt`. Fingerprint `0a6a1dce34181289` must not move (kernelPolicyVersion stays 1; `solverPolicyVersion` is a hello/manifest field, not in the fingerprint seed — verify).

VERIFY (run yourself, report counts): `ctest --test-dir worker/build -R 'solver|sketch|canonical_sketch|drag' --output-on-failure`, then FULL `ctest --test-dir worker/build`; `scripts/check-worker-stdout-hygiene.sh`; from `src-tauri/`: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `ONECAD_WORKER_PATH=$PWD/../worker/build/onecad-worker ONECAD_REQUIRE_WORKER=1 cargo test --test sketch_edit --test sketch_conflicts --test sketch_direct_manipulation --test sketch_on_face --test wire_contract --test sketch_regions`, `cargo test -p onecad-core --test edit_session --test regen_planner`, `cargo test -p onecad-protocol`, `cargo test --lib` (manifest rows).

REPORT: files changed with line counts; the §14 entry text; each fixture round and its measured output; each red-first failing assertion before the fix; which converged-with-residual case you could construct and how; every verify summary line; deviations; unverified items.
