GOAL: 2D fillet and trim behave like a professional sketcher (B4, B5, B7, B8): a filleted profile triangulates and finishes, trim remaps constraints instead of dropping them, fillet authors a Radius plus endpoint tangency, deleting a fillet arc restores the corner — with a new `TangentAtPoint` constraint kind across SCHEMA, worker, Rust and frontend, all red-first.

CONTEXT: repo /Users/andrejvysny/workspace/OneCAD. Read CLAUDE.md (§ Invariants, § C++ worker, § Rust, § TypeScript, § Testing), `PLAN.md` § Context "B4/B5/B7/B8", § Decisions D7, § amendments A-10/A-15, § "Protocol pre-audit and Astra derive outcomes" P-8 and P-11, § Design WP-3. The accepted derivation you IMPLEMENT is `docs/design/astra/sketch-fillet-fill-and-tangency.md` — read its header (scope decisions: what is accepted, what is rejected/deferred) and §3 (certified orientation predicate, merge/collinear pre-pass, endpoint tangency lowering + branch policy), §4 (degeneracy table), §5 (fill algorithm steps 1–10 and the tangency lowering sequence), §6 (F1–F6, T1–T5 vectors), §7 (counterexamples), §8 (interfaces, refusal codes, frontend authoring, trim remap rules).
Anchors — worker: `worker/src/loop/PolygonFill.cpp` (:16 closure check 1e-12, :94 inclusive `point_in_triangle`, :223-262 `ear_clip_indices`, :264-275 `fill_region` completeness rule), `worker/src/loop/LoopDetector.cpp` (:41 `kMinArea`, :86-97 chord tolerance, :194+ arc tessellation floors, :240 size-dependent coincidence tolerance, :2050 consecutive-duplicate removal — never compares last vs first), `LoopDetector.h:204-240` config, `worker/src/protocol/SolverLane.cpp:901-1033 on_regions` (refusal site :963-976), `worker/src/sketch/WireSketch.cpp:348+` (constraint parsing; :479 unknown type ⇒ loud error), `worker/src/sketch/solver/ConstraintSolver.cpp:108 makeArc` (Start/End unset — construct a complete arc for the endpoint form), `:1468-1590` Tangent lowering, `worker/third_party/planegcs/GCS.h:337 addConstraintAngleViaPoint`, `Constraints.cpp:2355` (AngleViaPoint error, radians; :2370 zero-normal behaviour), `worker/tests/test_sketch_arc_endpoints.cpp:112-210` (slot fixture; `test_endpoint_weld_makes_entity_tangent_redundant` stays true for entity-level `Tangent`), `worker/tests/test_region_table.cpp`, `test_sketch_region_diagnostics.cpp`. Rust: `src-tauri/src/worker/wire.rs:4500-4700` (constraint wire forms + `positions`), `onecad-core/src/sketch/mod.rs` (constraint kinds enum/validation), `src-tauri/tests/sketch_regions.rs`. Frontend: `src/tools/sketch/sketchService.ts` (:340-369 delete cascade, :385-423 trim, :467-512 `remapExtendConstraints` precedent, :579-740 fillet + `remapFilletConstraints`), `src/tools/sketch/sketchFilletMath.ts:131 filletGeometry` (arcEndA/arcEndB roles), `src/tools/sketch/trimMath.ts`, `src/tools/sketch/autoConstrain.ts` (tangent-arc chaining authors entity Tangent today — switch to the endpoint kind where the point is welded), `src/ipc/types.ts:221-238` (`SketchConstraintType` union), `src/ipc/sketchWireMap.ts` (constraint marshalling), `src/ipc/localSolver.ts` + `mockSketch.ts` (mock DOF arity table must learn the new kind: arity 1), `src/features/sketch/constraintCatalog.ts` (badge/label), `src/features/sketch/ConstraintBadgeLayer.tsx`. Tests: `src/tools/sketch/sketchService.trim.test.ts`, `sketchFilletMath.test.ts`, `SketchController.filletOffset.test.ts`, `e2e/sketch-fillet.spec.ts`, `e2e/sketch-trim.spec.ts`, `protocol/fixtures/` (+ `worker/tests/CMakeLists.txt` `canonical_*` block ~:1104-1118), `scripts/verify-modeling-contracts.mjs` / `docs/qa/modeling-operation-contracts.json` (check whether constraint kinds are enumerated there).

ORDER OF WORK (binding):
1. **B4 reproduction FIRST (A-15).** Write a vitest in `src/tools/sketch/` that drives the FE math for the review's shape (rectangle 30×20; circle Ø25 centred on the midpoint of the bottom 30 mm edge; trim the outer circle arc so the inward semicircle remains; 2D fillet r = 5 on a rectangle corner that still has ≥ 5 mm legs — per A3 §6 F4) and dumps the resulting entity + constraint JSON to a fixture file `worker/tests/fixtures/sketch_fillet_profile.ndjson` (SketchUpsert + SketchRegions rounds). Run it through the worker harness and record the EXACT outcome (complete fill, or the `incomplete triangulation` refusal with the detector flags, polygon order, first–last distance, and every stalled ear's determinant per A3 §9 "Required F4 probe evidence" — add a temporary stderr probe if needed, remove it after). If the refusal does not reproduce with that shape, try the corner adjacent to a circle–edge intersection and the sub-tolerance closing-seam variant (append a closing representative 5e-5 mm off). Report what reproduced. Only then choose the fix site.
2. **Fill hardening (P-11 scope):** `worker/src/loop/PolygonFill.cpp`: `orient2d_certified(a,b,c)` = filtered determinant with bound `ε_area = γ₈·S` (γ₈ = 8u/(1−8u), u = 2⁻⁵³, S = |x_x·y_y| + |x_y·y_x|) and an EXACT fallback for `|D| ≤ ε_area` (implement exact sign via error-free transformations — TwoProduct/TwoSum (Dekker/Knuth) on the four products, or a compact Shewchuk-style expansion; document the algorithm and its assumptions: no overflow/underflow, IEEE binary64); `point_in_triangle` re-expressed through it (inclusive); pre-pass: remove exact closure duplicates and consecutive exact duplicates; exact forward-collinear chains (`orient == 0 ∧ (b−a)·(c−b) > 0`) simplified temporarily and RESTORED after clipping by subdividing the adjacent triangle; NO positive geometric cutoff, NO proximity merging in the fill (that belongs to the detector with provenance — out of scope); post-fill verification (T = n − 2 with bridge occurrences; every triangle positively oriented; required boundary edges single-use; every interior/bridge edge exactly two-incident with opposite directions; area equality vs the certified signed area within floating error; f32 encoding check — no zero-length edge after conversion); refusal with a named code `FILL_EAR_STALLED | FILL_BOUNDARY_OVERLAP | FILL_BOUNDARY_PINCH | FILL_AREA_BELOW_MINIMUM | FILL_RING_CONTRACT_FAILED | FILL_F32_TOPOLOGY_LOSS | FILL_NONFINITE_INPUT` carried as the `SketchRegions` `OP_FAILED` reasonCode with evidence (region id, ring/vertex index, determinant, bound, exact sign, blocker). The loop area floor becomes ONE named policy shared with the detector's `kMinArea`. Keep the shipped bridge construction; the verification covers it.
3. **`TangentAtPoint` (P-8):** SCHEMA §7.3 constraint catalogue: 21st kind `TangentAtPoint { entities: [curveA, curveB], positions: [roleA, roleB] }` (roles name the shared endpoint on each operand: `Start|End` for lines/arcs; the two roles must resolve to a structurally welded point — a `Coincident` between them must exist), arity 1, residual in RADIANS (own-dimension rule; angular tolerance per A2: τ_len / max(line span, arc radius)); §7.4 residual prose amended; §14 entry per the auditor skeleton in `PLAN.md` § Protocol pre-audit "WP-3b"; the "the 20 kinds" sentence becomes 21. Worker: `WireSketch` parses it; `ConstraintSolver` lowers it to ONE `addConstraintAngleViaPoint(curve1, curve2, T, &α, userTag, true)` with α ∈ {0, π} chosen ONCE at lowering from the actual PlaneGCS normals at T (A3 §3 "Branch handling": aligned ⇒ 0, opposed ⇒ π; store α in stable memory, excluded from unknowns; reject zero normals / ambiguous seed with `TANGENT_BRANCH_AMBIGUOUS`, unresolved endpoint `TANGENT_ENDPOINT_UNRESOLVED`, not welded `TANGENT_ENDPOINT_NOT_WELDED`, degenerate operand `TANGENT_DEGENERATE_OPERAND`); arcs constructed COMPLETE (center, radius, angles, start, end) for this path; residual reader added to the residual gate table (if WP-2's `ResidualGate` has landed in the tree, extend it; otherwise add the reader where `maxConstraintResidual` reads and note it for the WP-2 merge). Rust: kind in the `onecad-core` sketch model + validation (positions required, both roles), wire round-trip, DTO/`types.ts`/`sketchWireMap.ts` lockstep, mock arity table. Fixture `protocol/fixtures/sketch_tangent_endpoint.ndjson`: T1 (DOF 9 → 8 → 7 → 6 with Radius), T5 slot (four independent rows 9→5), a drag that keeps tangency (BeginGesture/SolveDrag/EndGesture round with the welded endpoint dragged; assert the angular residual ≤ τ_ang after EndGesture), a not-welded refusal.
4. **Frontend authoring (D7 / A3 §8):** fillet authors atomically 2 Coincident + 2 `TangentAtPoint` (roles from `arcEndA/arcEndB` and `aEnd/bEnd`) + `Radius(arc, typed r)` + `remapFilletConstraints` survivors; `remapTrimConstraints` (new, beside `remapExtendConstraints`): Radius/Diameter → every surviving piece of the same circle support (same value); Coincident/Concentric/Equal/OnCurve/`TangentAtPoint` whose contact endpoint survives → the piece keeping that exact endpoint; a tangent whose contact endpoint was consumed → dropped WITH a status hint naming it; Midpoint/Distance/Angle/HDistance/VDistance on the trimmed entity → dropped with the hint listing them ("Trim removed Midpoint, Distance 30 mm"); never silent. Delete: when a doomed Arc is `TangentAtPoint`-welded at both ends to two lines that are not parallel → extend both legs to their intersection (reject if the intersection is farther than 10× the arc radius from either tangent point ⇒ plain delete + hint), weld them with `Coincident`, drop the arc's constraints, hint "Corner restored"; a plain arc (no tangency) deletes as today. `autoConstrain` tangent-arc chaining: author `TangentAtPoint` when the chain point is welded, entity `Tangent` otherwise. Badge/catalog label "Tangent at point".
5. Tests (red-first): FE `sketchService.fillet.test.ts` (new), `sketchService.trim.test.ts` (remap table rows), `sketchService.delete.test.ts` (unfillet + the 10× refusal), `autoConstrain.test.ts`; e2e `sketch-fillet.spec.ts` gains "fillet → finish → 1 region" and "delete fillet arc → corner restored → 1 region", `sketch-trim.spec.ts` gains "trim keeps Ø25"; Rust `sketch_regions.rs` (the fillet profile from step 1 triangulates; `TangentAtPoint` round-trips); worker `test_polygon_fill_certified.cpp` (A3 §6 fill vectors F1, F2 ×3 legs, F3 170° + acute variant, F4 upper cell, F5 hole bridge, F6 ×3, exact collinear chain 5 vertices/3 triangles, closure duplicate, near-closure ⇒ refusal, the §7 counterexamples: the 0.02 mm step preserved, the 3000/6000/2⁻⁴⁰ exact-sign case classified convex, the f32 10 000 mm case refused) and `test_tangent_at_point.cpp` (T1–T5 rank sequences, branch selection, reversed line parameterisation, perturbation ±0.001 rad).

CONSTRAINTS: you own the worker build tree for this package (the previous worker package has finished — confirm `git log -1` mentions WP-2 or ask the orchestrator before building); `ONECAD_OCCT_ROOT=$HOME/.onecad-occt/8.0.1 scripts/build-worker.sh Release` before cargo. Do not edit `corpus/`, `mesh_format.md`, contracts/goldens; keep `test_endpoint_weld_makes_entity_tangent_redundant` green (entity-level Tangent semantics unchanged); C++20 `-Wall -Wextra`, `WLOG_*` only, stdout hygiene; `onecad-core` tauri-free; clippy `-D warnings`; no `any`, no raw hex; fingerprint `0a6a1dce34181289` unchanged (kernelPolicyVersion stays 1 — the fill change alters no published fill for polygons that already triangulated; if a `canonical_*` replay's `previewTriangles` bytes move, STOP and report — that needs a recorded decision).

VERIFY: `ctest --test-dir worker/build -R 'region|fill|tangent|arc_endpoints|canonical_sketch' --output-on-failure` then FULL ctest; `scripts/check-worker-stdout-hygiene.sh`; from `src-tauri/`: fmt, clippy, `ONECAD_WORKER_PATH=$PWD/../worker/build/onecad-worker ONECAD_REQUIRE_WORKER=1 cargo test --test sketch_regions --test wire_contract --test sketch_edit`, `cargo test -p onecad-core`, `cargo test -p onecad-protocol`; `bunx tsc --noEmit`; `bunx vitest run src/tools/sketch src/ipc src/features/sketch`; `bunx playwright test e2e/sketch-fillet.spec.ts e2e/sketch-trim.spec.ts --project=chromium`; `node scripts/verify-modeling-contracts.mjs`.

REPORT: the B4 reproduction outcome (exact refusal text and probe evidence, or "did not reproduce" with the shapes tried) BEFORE the fix description; files changed with counts; §14 text; fixture rounds + measured outputs; each red-first failing assertion; every verify summary line; deviations; unverified items.
