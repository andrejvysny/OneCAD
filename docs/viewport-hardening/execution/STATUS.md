# VP-HARDENING 1.0 — execution ledger

This file is the live execution ledger for the viewport hardening program. It records evidence, not intent. Every gate line points at a log under `docs/qa/viewport-hardening/`. The template it was copied from is `../07-EXECUTION-LEDGER-TEMPLATE.md`.

## 1. Session identity

| Field | Value |
|---|---|
| Program/spec version | VP-HARDENING 1.0 |
| Design baseline | `65b4c60eb226a201f2fa3eb565d7283b1c63b5ac` |
| Actual local HEAD | `65b4c60eb226a201f2fa3eb565d7283b1c63b5ac` (identical to the design baseline) |
| Branch / checkout | branch `viewport-hardening`, git worktree `/Users/andrejvysny/workspace/viewport-hardening`. The main checkout at `../OneCAD` is on `master` with a dirty tree owned by another session; it is not touched by this program. |
| Pre-existing changed/untracked files | Only `?? docs/viewport-hardening/` (this package). `git diff` empty. |
| Dirty-diff hash at start | `e3b0c442…b855` (sha256 of empty diff) |
| Claude Code version | 2.1.270 |
| Main model / subagent models | Fable 5.1 (`claude-fable-5-1`) orchestrating; scouts and implementers on Sonnet 5 / Opus 5 via the Agent tool `model` parameter; observed routing recorded per brief |
| Bun / Node / TypeScript / Three.js | bun 1.4.0 · node 22.23.2 · typescript ~5.8.3 · three 0.185.1 + @types/three 0.185.1 (from `bun.lock`; `bun install --frozen-lockfile` 597 packages, exit 0, `baseline/bun-install.log`) |
| Rust / CMake / compiler / OCCT | rustc 1.97.0 · cargo 1.97.0 · cmake 4.1.3 · ninja 1.13.1 · Apple clang (Xcode) · OCCT 8.0.1 at `~/.onecad-occt/8.0.1`, fingerprint `0a6a1dce34181289`, build id `local` |
| Worker binary | `worker/build/onecad-worker` built 2026-09-13 17:34 local, sha256 `14741a45ae0b1aeb2e7e6581bfd5596460632775566b8d751c9816e03d86b1a6`, staged to `src-tauri/binaries/onecad-worker-aarch64-apple-darwin` (`baseline/worker-build.log`, exit 0). ctest inventory: 197 targets. |
| Native machine and display | MacBook Pro, Apple M4 Pro, macOS 26.6.2 (25G83), built-in Liquid Retina XDR 3024×1964 (DPR 2). Power mode and external displays: not recorded yet. |
| Active heavy build process / owner | See §3 heavy lane |
| three-mesh-bvh | published `0.9.15`, peer `three >= 0.159.0` (queried via `bun pm view` 2026-09-13). Not installed. Pinning deferred to WP14 per guide §5 step 7. |

## 2. Package status

States: `not-started`, `reproducing`, `implementing`, `focused-pass`, `integrated-pass`, `native-accepted`, `blocked-native`, `blocked-device`.

| Package | State | Focused evidence | Integrated/native evidence | Owner / next action |
|---|---|---|---|---|
| WP00 inventory | focused-pass | §5.1 baseline gates, §5.2 G-lane probes, U-lane ratchets (`vph-counterexamples-red.log`) | e2e probes ran in real Chromium WebGL (mock backend) | W-lane S-curve red owed to WP08's first step |
| WP01 line units | focused-pass (U) | adversarial review: no blocking findings, 8 non-blocking (all applied inline: zero-size guard in `updateMetrics`, `floor` buffer dims, aspect follow on DPR path, threshold comment, vacuous snap test replaced, `beginPick` try/finally); orchestrator rerun after fixes: see §7 WP01 record | G: line-width spec `test.fail` removed, rerun owed when the heavy lane frees; TEST-LINE-02 G, TEST-LINE-04, DPR 1.5 / dark theme / perspective variants owed | next: WP02 (same owner file) |
| WP02 lifecycle | focused-pass (U) — consolidation 2026-09-14: PR-05/PR-11 closed (in-tick invalidate deferred, windowed park bound, listener isolation, held reasons, submission-matched ack); adversarial review R1(b) sound-with-conditions, all findings fixed | `runs/consolidation-fe-focused/manifest.json` | G/N: TEST-LIFE-03, TEST-BACKEND-02 owed | next: WP06 |
| WP03 ownership | focused-pass (U + G) — round 2 CLOSED 2026-09-14 (N-2 preview handles disposed + sweep deleted, N-3 section lease by object, N-8 ghosts lease/own, DEV-WP03-1 outline+count, N-4 planned capacity + receipts); R1(b) findings fixed | `runs/consolidation-fe-focused/manifest.json`, `runs/consolidation-g-lane/manifest.json` (1,000 hovers flat at 13 geometries; 100 preview + 50 document cycles flat) | N owed | next: WP05, WP11 |
| WP04 validation | focused-pass (U) — consolidation 2026-09-14: PR-01 proof-carrying promotion on every viewport lane, PR-02 two-phase publish, PR-03 preparation plan + lifetime reservations + preview admission; R1(a) DEFECTIVE → all findings fixed | `runs/consolidation-fe-focused/manifest.json` (TEST-PUB-03 added) | X lane and N owed | next: WP07 |
| WP05 appearance | not-started | not-run | not-run | after WP03 |
| WP06 camera/input | not-started | not-run | not-run | after WP01 |
| WP07 picking | not-started | not-run | not-run | after WP04, WP06 |
| WP08 curve sampling | focused-pass (W) — round 3 LANDED 2026-09-14 (C1/C2/F5/F6/F7/F9) + fix round from Astra `verify` (call 3/3, `docs/design/astra/wp08-curve-sampler-verify.md`) and R1(c): emitted-turn rule with no noise gate, J interval, no reversal-based singular exemption, underflow refusal, enclosure charged into E | `runs/consolidation-worker-focused/manifest.json` | Rust workspace + N owed | Astra budget 3/3 used; interval-arithmetic enclosures → WP12 |
| WP09 normals | focused-pass (W) — reviewed 2026-09-14: Astra `break` (call 1/3, `docs/design/astra/wp09-surface-normals-break.md`, DEFECTIVE → F1–F11 fixed: `ok=false` on a missing nondegenerate face, certificate-based degeneracy, representation-derived derivative budget in parameter units, `Unresolved` provenance, symmetric triangle predicate, trusted-metadata tangency, analytic pole sign, cone-apex split, valence bound) + R1(c) fixes (extrusion/revolution/offset keep surface normals as `Unresolved`; `Tessellate` verb returns `OP_FAILED` with the diagnostic) | `runs/consolidation-worker-focused/manifest.json` | N owed | next: WP10 |
| WP10 protocol/origins | not-started | not-run | not-run | after WP08, WP09; single cross-layer owner |
| WP11 sketch resources | not-started | not-run | not-run | after WP01, WP03 |
| WP12 shared quality | not-started | not-run | not-run | after WP10, WP11 |
| WP13 sections/edges | not-started | not-run | not-run | after WP05, WP07, WP10 |
| WP14 preparation/BVH | not-started | not-run | not-run | after WP10, WP11 |
| WP15 finishing | not-started | not-run | not-run | after WP06, WP12 |
| WP16 qualification | not-started | not-run | not-run | after WP13, WP14, WP15 |

## 3. File ownership and heavy-lane lease

| Stream | Package | Exact owned paths | Shared seam / integration owner | Start checkpoint | State |
|---|---|---|---|---|---|
| orchestrator | WP00 | `docs/viewport-hardening/execution/**`, `docs/qa/viewport-hardening/**`, `src/viewport/vph/**`, `docs/adr/0015-*`, `scripts/verify-viewport-acceptance.mjs`; instrumentation hooks in `ViewportEngine.ts`, `Picker.ts`, `meshRegistry.ts` (landed before the WP01/WP04 leases below) | — | 65b4c60 clean | active |
| impl-standard (Sonnet) | WP00 G-lane probes | `e2e/vph-line-width.spec.ts`, `e2e/vph-hover-lifecycle.spec.ts` | none | 65b4c60 + WP00 instrumentation | running |
| impl-careful (Opus) | WP01 | `src/viewport/engine/{dpr,ViewportMetrics,screenLineStyle,dprWatcher,bodyMaterials,HighlightLayer,SketchObject,SketchStaticLayer,SnapIndicator,OriginTriad,Picker,ViewportEngine}.ts`, `engine/README.md`, colocated tests, one case in `vph/baselineCounterexamples.test.ts` | `ViewportEngine.ts`, `SketchObject.ts`, `Picker.ts` single-owner → WP01 | 65b4c60 + WP00 instrumentation | running |
| impl-critical (Opus) | WP04 | `src/viewport/mesh/**` (+ new `validateMesh.ts`, `meshAdmission.ts`, `meshMutations.ts`, `previewMesh.ts`), preview call sites in `ModelToolController.ts` / `placementController.ts`, `acceptance-matrix.json`, `runs/wp04-focused/` | `meshSync.ts` single-owner → WP04; promotion gate wired by orchestrator in `src/ipc/promote.ts` | 65b4c60 + WP00 instrumentation | done (focused-pass) |
| impl-careful (Opus) | WP02 | `src/viewport/engine/{FrameScheduler,ViewportEngine,renderer}.ts` (+ tests), `ViewportRoot.tsx` (lifecycle hint), `SettingsModal.tsx` (toggle), R06 case in `vph/baselineCounterexamples.test.ts`, `engine/README.md` | `ViewportEngine.ts` single-owner → WP02 | after WP01 | done, under adversarial review |
| impl-critical (Opus) | WP08 | `worker/src/tess/{CurveSampler.h,CurveSampler.cpp,Tessellate.cpp}`, `worker/CMakeLists.txt` (source list), `worker/tests/{test_curve_sampler.cpp,CMakeLists.txt}`, `baseline/ctest-curve-sampler-red.log`, `runs/wp08-focused/` | worker tessellation single-owner → WP08 | after WP00 | done, under adversarial review + Astra `break` (call 1 of ~3, packet 96ad6116d09d) |
| impl-careful (Opus) | WP03 | `src/viewport/mesh/{meshRegistry,renderResourceIdentity,faceSliceGeometry,highlightCache}.ts`, `engine/{HighlightLayer,BodyObject,SectionLayer}.ts`, `meshSync.ts` (handle dispose only), `vph/instrumentation.ts` (counters), tests, matrix rows TEST-RES-*, `runs/wp03-focused/` | `meshSync.ts` → WP03 (narrow); `ViewportEngine.ts` frozen under WP02 review | after WP01 + WP04 | running |

Heavy lane (one process at a time):

| When (local) | Owner | Command | Working dir | Output | Result |
|---|---|---|---|---|---|
| 2026-09-13 17:29–17:34 | orchestrator | `ONECAD_OCCT_ROOT=~/.onecad-occt/8.0.1 scripts/build-worker.sh Release` | repo root | `baseline/worker-build.log` | exit 0 |
| 2026-09-13 17:35–17:38 | orchestrator | serial: `bun run test` → `ctest --test-dir worker/build` → `cargo fmt --check` → `cargo clippy -D warnings` → `ONECAD_REQUIRE_WORKER=1 cargo test --workspace` | repo root / `src-tauri` | `baseline/gates-run.log` + per-gate logs | done (§5.1) |
| 2026-09-13 17:38–17:40 | orchestrator | `ONECAD_REQUIRE_WORKER=1 cargo test --workspace --no-fail-fast` | `src-tauri` | `baseline/cargo-test-nofailfast.log` | done (§5.1) |
| 2026-09-13 ~18:05–18:25 | impl-standard (Sonnet) G-lane probe author | `E2E_PORT=4179 bunx playwright test e2e/vph-line-width.spec.ts e2e/vph-hover-lifecycle.spec.ts --project=chromium` | repo root | `baseline/vph-g-lane-probes.log` | done (measured against WP01 in flight — see §5.2) |
| 2026-09-13 18:4x | orchestrator | `E2E_PORT=4181 bunx playwright test …` in a detached scratch worktree of `65b4c60` | scratchpad worktree (removed after) | `baseline/vph-g-lane-probes-at-65b4c60.log` | done |
| 2026-09-13 18:5x | orchestrator | `E2E_PORT=4179 bunx playwright test e2e/vph-line-width.spec.ts e2e/vph-hover-lifecycle.spec.ts --project=chromium` | repo root | `runs/wp01-focused/playwright-chromium.log` | done (§5.2 post-WP01) |
| 2026-09-13 ~19:0x–19:4x | impl-critical WP08 (rounds 1–2) | `scripts/build-worker.sh Release`; `ctest -R …`; full `ctest` (round 2) | repo root | `runs/wp08-focused/ctest.log`, `ctest-round2.log` | done |
| 2026-09-13 21:0x– | orchestrator | full `ctest --test-dir worker/build` then `ONECAD_REQUIRE_WORKER=1 cargo test --test step_import_gate --test project_edges --test preview_edge_shell --test topology_rebind --test m2_gate --test real_worker_smoke --test breadth_ops` | repo root / `src-tauri` | `runs/wp08-focused/ctest-full-orchestrator.log`, `cargo-edge-consumers.log` | running — NOTE: overlaps the probe author's single Playwright spec run (`playwright-linewidth-phased.log`); the width measurement is not timing-sensitive, but any odd ctest red is re-run in isolation before attribution |

Single-owner integration files (never two writers): `ViewportEngine.ts`, `ViewportRoot.tsx`, `meshSync.ts`, `SketchObject.ts`, `settingsStore.ts`, `protocol/SCHEMA.md`, `protocol/mesh_format.md`, Rust DTOs (`src-tauri/src/dto.rs`), worker Dispatcher.

## 4. Baseline findings map (R01–R18)

Populated by WP00 from a read of the actual source at HEAD. HEAD equals the review baseline, so no finding can be "fixed by newer source" unless the review itself misread the code; each row therefore records what the source at `65b4c60` actually does.

Verdicts: `present` = the mechanism the review describes is in the source and the counterexample reproduces on it; `present-unconfirmed` = mechanism in source, effect needs a G/N lane to observe; `design-gap` = nothing to "fix", the capability does not exist yet; `not-a-defect` = the review's reading is not borne out by source. Red reproductions: `src/viewport/vph/baselineCounterexamples.test.ts` (each case `it.fails` until its package flips it), first red run `baseline/vph-counterexamples-red.log` (9 red of 11; one of the nine was a wrong oracle in the test itself, corrected — see the float32 case).

| Finding | Verdict | Source at 65b4c60 | Red reproduction | Package |
|---|---|---|---|---|
| R01 line units | present-unconfirmed (mechanism proven in installed dependency) | `engine/dpr.ts:33` `cssToDevice = cssPx * dpr`; `bodyMaterials.ts:124,129`, `HighlightLayer.ts:121`, `SketchObject.ts:456` (one `mk()` for 13 materials), `SketchStaticLayer.ts:224`, `SnapIndicator.ts:493`, `OriginTriad.ts:116` all feed device-px widths; `Picker.ts:517-527` flushes device-px resolution; installed `three/examples/jsm/lines/LineSegments2.js:419-428` overwrites `resolution` from `renderer.getViewport()`, which `WebGLRenderer.js:781-785` stores in logical px. Gizmos, section, datum use no fat lines. | U: installed-contract test green; `cssToDevice(1.25,2)` red. G: probe owed (WP01) | WP01 |
| R02 highlight ownership | present | `HighlightLayer.ts:238-246` shared-attribute wrappers, `:253-267` never disposes face/body wrappers; `HighlightLayer.test.ts:208` encodes it. Marker attributes replaced without dispose at `SketchObject.ts:731,900,908,920`. | none pure (needs renderer bookkeeping) — G-lane hover loop owed | WP03 |
| R03 edge sampler | present | `worker/src/tess/Tessellate.cpp:110-135` midpoint + endpoint-tangent, `kMaxDepth=16` silently accepts at the cap; lines bypass. | W: ctest owed (WP08) — S-curve must be an OCCT edge fixture | WP08 |
| R04 normals | present | `Tessellate.cpp:225-244` area-weighted triangle normals per face, `(0,0,1)` fallback at L243; `mesher.Perform()` after constructor at L172-173; no UV/surface evaluation. Duplicated verbatim in `tessellate_raw` L320-392. | W: owed (WP09) | WP09 |
| R05 material state | present | `bodyMaterials.ts:145-152` `setDimmed` iterates `sets` only; `assemblySets` created while dimmed (L92) never restored; state keyed by `MaterialKind`. `vertexColorKind('assemblyColor') = shadedVertex` (`renderModes.ts:48-56`) — product decision now fixed by D08: assembly overrides authored colors. | U: owed (WP05) | WP05 |
| R06 scheduler | present (scout misread it; reproduction confirms) | `ViewportEngine.ts:802-810` `tick()` runs `renderFrame()` then `dirty=false`; a listener invalidating during the frame schedules a rAF that then renders nothing. No submit/present distinction in telemetry naming (none exists). | U red: `R06 scheduler` case — frameCount stays 1 | WP02 |
| R07 camera | present | `CameraRig.ts:73-77` `setAspect` perspective-only, ortho extents only in `apply()`; `CadOrbitControls.ts:467-476` moves target before clamping distance; `onPointerDown` L305-311 cancels tween, wheel/gesture do not; `isDragActive` gates wheel orbit only (L403-407). Camera state `{yaw,pitch,distance,target}` lives only in the controls; nothing persisted anywhere (no store, no DTO, no wire) — migration has no stored shape to preserve, only the live default (`fov 76`, `distance 260`, ISO). | U red: ortho aspect; zoom-at-min target drift 0.072 mm | WP06 |
| R08 sketch rebuilds | present | `SketchObject.ts:673-684` `setSelection`/`setHover` → `rebuildEntities()` (L850-894) disposes/recreates every entity line; `setPreview`/`setTrimGhost` (L597-644) allocate fresh geometry per call. | U: owed (WP11) | WP11 |
| R09 sketch quality | present | `SketchObject.ts:56` `ARC_SEGMENTS=64` fixed path used by preview/trim (L601-636) and `SketchStaticLayer.ts:194`; session-max trigger `wantedSegments()` L1076-1080; screen metric `ViewportEngine.ts:872-891` max column length, not σmax. | U red: sagitta 1.2045 px at r=1000; global-max says no rebuild | WP12 |
| R10 pick depth bias | present | `Picker.ts:74-89` `linePickThreshold` world units at focus distance; `:119-129` `choosePreferredHit` uses it as depth allowance; `:445-453`. | U red: edge at 261 wins over face at 260 with bias 2.4376 | WP07 |
| R11 mesh validation | present | `parseMeshPayload.ts` validates structure only (L151-258, L317-330); no finite/index-bound/range-contiguity/ID-offset/UTF-8 checks; `meshRegistry.ts:208-240` allocates edge expansion from unvalidated ranges; Rust `onecad-protocol/src/mesh.rs:198-294` header-only by design; `Tessellate.cpp:198-204, 282-285` silently emit zero ranges for missing faces / unsamplable edges. Fences: `MeshProvenance {documentId, runtimeSession, snapshotId, generation}` (`meshRegistry.ts:70-75`, `meshSync.ts:313-336`). | U: adversarial fixtures owed (WP04) | WP04/WP09 |
| R12 precision envelope | present | `CameraRig.ts:17-18` near 0.1 / far 100000; `cameraFit.ts:4-5` 0.5–50000 with impossible-fit refusal (L36-75, keep); `Tessellate.cpp:236-238` `static_cast<float>` of world doubles; `meshSync.ts:49` fixed `"fine"` tier; wire LOD is the string enum `coarse|medium|fine` (`SCHEMA.md` §7.6), header `lod` u16. | U green: float32 spacing characterization | WP06/WP10/WP12 |
| R13 edge classes | design-gap | No seam/continuity metadata anywhere in `Tessellate.*`/`Mesh1.h`; `renderModes.ts:58` modes `shaded|shadedEdges|wireframe|assemblyColors`. | — | WP13 |
| R14 render policy | present (partly by-design) | `SectionLayer.ts` hides the cap while `sketchActive`; `ViewportEngine.ts:2251-2284` `previewHiddenBodies` saved-boolean map; `HighlightLayer.ts:86,114-117` edge overlays `depthTest:false, depthWrite:false` (explicit, fine); `renderOrder.ts` single ladder (keep). Opacity-based dimming is the D09 change. | G/N owed (WP13) | WP05/WP13 |
| R15 scale | design-gap | `Picker.ts:456-502` brute-force `traverseVisible` + `intersectObjects`; no BVH/octree in `src/viewport`; `FACE_BBOXES` (type 11) parsed but unused; `faceColors.ts` full de-index whenever colors present. | B owed (WP14) | WP14 |
| R16 input/grid | present | `CadOrbitControls.ts:306` `setPointerCapture` before any ownership decision; two-pointer pan applies each pointer's own delta (L321-323); no `pointerType` branch; `pointercancel` aliased to `onPointerUp`, no `lostpointercapture`; `GridPlane.ts:53-56` step from camera distance, finite 100-cell `LineSegments`. Sketch entry saves/restores a `{yaw,pitch,distance,target}` snapshot (`ViewportEngine.ts:1436, 1720`), i.e. yaw/pitch only — cannot express a tilted plane basis. | U: owed (WP06/WP15) | WP06/WP15 |
| R17 backend | present | `renderer.ts:78-83` attrs (`preserveDrawingBuffer`, `stencil`, no explicit `outputColorSpace`, no reversed-depth option), L134-146 WebGPU flag path with fallback; `settingsStore.ts:151` `experimentalWebGpu`, persist version 11; `captureThumbnail` `ViewportEngine.ts:974-991` forces a sync frame. | U: owed (WP02) | WP02/WP15 |
| R18 face-color ordinal | present (confirmed) | `faceColors.ts:124-125` `topo.idAt(f)` for a face ordinal; `faceRangeIndex.ts:58-75` `idOf` vs `idAt`; live path `meshRegistry.ts:160,198`; every existing fixture is one/two-triangles-per-face or single-face, so the bug is invisible there. | U red: faces 2/7/1 — f:1 receives red, f:2 receives green | WP05 |

MESH1 section codes 13–21 are unallocated at HEAD (`protocol/mesh_format.md` §4 defines 1–12 only); no collision for NUM §9.2.

## 5. Test execution records

Records live per run under `docs/qa/viewport-hardening/runs/<run-id>/` and are summarized here.

### 5.1 Baseline gate run — 2026-09-13 15:35–15:40 UTC, HEAD `65b4c60`, dirty diff empty

All logs under `docs/qa/viewport-hardening/baseline/`. These are the state of the tree BEFORE any viewport-hardening source change; every red below is pre-existing and owned outside this program (see ADR-0015 §5). The program reports whether it changes these numbers; it does not repair or delete them.

| Gate | Command | Exit | Measured | Log | What it does not prove |
|---|---|---:|---|---|---|
| TypeScript | `bunx tsc --noEmit` | 0 | clean | (inline) | runtime behaviour |
| Vitest full | `bun run test` | 1 | **346 files: 36 failed / 310 passed · 6065 tests: 443 failed / 5544 passed / 78 skipped**. 316 of the 443 are `TypeError: this.client.onDocumentChanged is not a function` from `ModelToolController.*.test.ts` fake clients missing the seam added in `ee5b449`; the remainder are related controller/inspector contract tests. No viewport engine/mesh test is among the failures. | `vitest-full.log` | GPU pixels, native input |
| Worker CTest | `ctest --test-dir worker/build --output-on-failure` | 8 | **195 / 197 passed**; failed: `feature_pattern` (#83), `chamfer_reference_face` (#190) — both recorded as open in `TODO.md` 2026-09-12 | `ctest-full.log` | frontend rendering |
| cargo fmt | `cargo fmt --all --check` | 1 | one diff, `src-tauri/tests/feature_pattern_integration.rs:108` | `cargo-fmt.log` | — |
| cargo clippy | `cargo clippy --workspace --all-targets -- -D warnings` | 0 | clean | `cargo-clippy.log` | newest-stable lints |
| cargo test (fail-fast) | `ONECAD_REQUIRE_WORKER=1 cargo test --workspace` | 101 | stopped after lib crate: 479 passed / 1 failed (`feature_pattern_repair_fixture_replays_through_strict_repair_parser`) | `cargo-test.log` | integration targets |
| cargo test (no-fail-fast) | `ONECAD_REQUIRE_WORKER=1 cargo test --workspace --no-fail-fast` | 101 | **100 result lines: 1627 passed / 24 failed / 0 ignored; 0 worker skips.** Failures are deterministic assertions (auto-bind / NeedsRepair expectations) in `hole_ops` (14), `preview_edge_shell` (2), `breadth_ops::shell_box_open_top`, `chamfer_reference`, `offset_face`, `revolve_*` (2), `transform_*` (3), plus the lib fixture above. Worker binary `14741a45…b1a6`, fingerprint `0a6a1dce34181289`. | `cargo-test-nofailfast.log` | that the failures are attributable to the worker build rather than the tree — no comparison run against the main checkout's binary was made |
| Playwright e2e | not run at baseline | — | deferred: ~26 min per project; will be run at the first UI-visible package (WP01) with the line-width probe | — | — |

Acceptance matrix: `docs/qa/viewport-hardening/acceptance-matrix.json` generated from the plan (127 IDs, all `not-started`); `node scripts/verify-viewport-acceptance.mjs` exit 0.

### 5.2 G-lane baseline probes (real Chromium WebGL via SwiftShader, mock backend)

Specs: `e2e/vph-line-width.spec.ts` (TEST-LINE-01) and `e2e/vph-hover-lifecycle.spec.ts` (TEST-RES-01), authored under WP00. Method per ACCEPTANCE §3.1: top orthographic view of the mock box, integrated contrast coverage across three scan rows through a vertical body edge, divided by the renderer DPR; MSAA 4 samples, `antialias:true`, `preserveDrawingBuffer:true`.

**Baseline measurement at exactly `65b4c60`** — run in a temporary detached worktree of that commit (created and removed in the scratchpad, `E2E_PORT=4181`), log `baseline/vph-g-lane-probes-at-65b4c60.log`:

| Case | Measured | Verdict |
|---|---|---|
| DPR 1 body edge | coverage 1.00 device px → **1.00 CSS px** (authored 1.25; Δ 0.25, at tolerance) | pass |
| DPR 2 body edge | coverage 4.51 device px → **2.25 CSS px** (authored 1.25) | expected fail (R01 reproduced: ~1.8× too wide) |
| DPR independence | 1.00 vs 2.25–2.35 CSS px | expected fail (R01) |
| 300 alternating rendered hovers | renderer geometries 11 → 611, **+2 per hover, no plateau**; `pickQueries` rose 11 918 → 12 518; `meshEntriesBuilt` flat at 2 | expected fail (R02 reproduced) |

The first run of the same specs in THIS worktree (`baseline/vph-g-lane-probes.log`, `E2E_PORT=4179`) happened while WP01 was mid-edit and measured 1.09–1.11 CSS px at DPR 2 — that log is evidence for WP01's direction, not a baseline, and the two `test.fail` annotations in `vph-line-width.spec.ts` will be removed when WP01 is accepted.

**Post-WP01 rerun** (`runs/wp01-focused/playwright-chromium.log`, `E2E_PORT=4179`, chromium): DPR 1 edge 1.00 CSS px (pass, at tolerance), DPR 2 edge **1.09 CSS px** (pass; was 2.25 at baseline — R01 fixed at the pixel level), hover-lifecycle expected-fail fired (R02 still open, geometries +2/hover). The DPR-independence case FAILED: inside that test DPR 1 measured 0.85 and DPR 2 1.11 CSS px (Δ 0.26 > 0.20). The DPR-1 number moves between 0.85 and 1.00 across runs for the same authored 1.25 px line, so the measurement is phase-sensitive at DPR 1 (a vertical edge samples one sub-pixel phase on every scan row; ACCEPTANCE §3.1 asks for repeated sub-pixel positions). This is a measurement-method defect in the probe, not a WP01 regression; fix owed: average over several fractional zoom offsets so both DPRs see multiple phases, then re-judge the 0.20 delta.

**Phase-averaged rerun** (`runs/wp01-focused/playwright-linewidth-phased.log`, five monotonic wheel steps −6…−18 ≈ 4.5 % zoom, mean of five phases): DPR 1 mean **1.044** CSS px (0.911–1.110), DPR 2 mean **0.955–1.011**, cross-DPR Δ **0.033** (pass) — line width is DPR-independent after WP01. The absolute width sits 0.15–0.35 px under the authored 1.25 at both DPRs, flipping the 0.25 tolerance run to run. Working hypothesis under test: the probe integrates coverage in gamma-space luminance while the AA/MSAA blend is linear-light, which under-counts every partial pixel by a constant; a linear-light rerun is in flight. Wheel-step calibration note for future phase sweeps: `ZOOM_SENS = 0.0015` makes ±30 wheel units far from tiny and alternating steps collapse to two states. Renderer string via `RENDERER` is Chromium's masked "WebKit WebGL"; `UNMASKED_RENDERER_WEBGL` is needed to name SwiftShader.

**Linear-light rerun** (`runs/wp01-focused/playwright-linewidth-linear-{1,2}.log`, SwiftShader named via `UNMASKED_RENDERER_WEBGL`: "ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device (LLVM 10.0.0)))", MSAA 4): coverage integrated in linear light (sRGB decode, Rec.709) reads consistently higher than gamma space (DPR 1: 1.009 vs 0.963; DPR 2: 1.016 vs 0.980), confirming the measurement-space bias. Results: DPR 2 mean **1.016 / 1.051 CSS px** (Δ 0.234 / 0.199 — inside the 0.25 tolerance), cross-DPR Δ **0.043 / 0.079** (pass). Two probe findings: (a) Playwright halves `deltaY` at `deviceScaleFactor: 2`, and a fractional wheel delta correctly flips the app's device classifier to trackpad (`navInput.weighEvidence`), turning later zooms into pans — the spec now uses even deltas; (b) the edge-locator ("leftmost below-threshold run") produced one 3.6 px outlier in run 2's standalone DPR-1 case while the same run's independence case read 0.996 — a locator robustness gap, owed. The orchestrator replaced its own 0.15 phase-spread upper bound (measured genuine spread 0.17) with the recorded-centres-moved check plus a 1.0 px gross-instability guard; the width assertions are unchanged. TEST-LINE-01 G is therefore green on width and cross-DPR, with the locator outlier as the open item.

Environment hazard recorded: port 4177 is held by an unrelated `vite` serving the main checkout `../OneCAD` (another session). Playwright's `reuseExistingServer` would silently test that app. Every Playwright run of this program passes an explicit `E2E_PORT` (4179 in this worktree) and this is noted in the heavy-lane table.

## 6. Deviation and risk record

### DEV-WP04-1 — consumer-side MESH1 v1 semantic rejections and eager id decoding
- Decision affected: D06 (validation) vs `protocol/mesh_format.md` §6.1 ("do not eagerly decode id strings … lazily on pick") and §4 (which lists no unit-normal, non-empty/≤256-byte id, per-namespace uniqueness, or exact range-coverage requirement).
- Evidence: adversarial review of WP04 (2026-09-13). NUM §10 requires fatal UTF-8 validation, non-empty ≤256-byte ids and per-namespace uniqueness, which cannot be established without reading the id bytes. Measured cost: 60 000-face body validates in 10.36 ms with all ids vs 2.20 ms with one id.
- Narrow correction: keep NUM §10 checks (they are the package's normative requirement, authority rank 3 over the review's rank-2 reading only where the wire document is silent; where `mesh_format.md` is explicit about lazy decode it is guidance, not byte semantics); record the consumer's validation contract in `protocol/mesh_format.md` as part of WP10's protocol pass (protocol-auditor before landing); revisit eager decode when WP14 moves preparation off the main thread.
- User-visible: none intended; a structurally valid v1 blob that violates a NUM §10 semantic invariant is now refused and the body shows a diagnostic instead of wrong geometry.
- Wire/persistence: none (bytes unchanged).
- Tests: `validateMesh.test.ts` TEST-MESH-01..04; owed: X-lane cross-track fixture proving a real worker blob passes (`TEST-MESH-06`/WP09 or WP10).
- Status: open until WP10 documents the contract.

### RISK-WP04-2 — header bbox vs real worker output (MESH1 v1)
- `Tessellate.cpp` computes the header bbox with `BRepBndLib::Add` BEFORE meshing; on re-tessellation OCCT may derive it from a previously attached coarser triangulation. Strict enclosure checking could therefore falsely reject a real body. Correction ordered: enclosure is measured and reported (`headerBoundsExcursionMm`), geometry bounds come from measured positions, strict enclosure returns for v2 (NUM §9.3). Owed: W/X check running a coarse-then-fine real worker blob through the validator.

### DEV-WP08-1 — angular criterion: "negligible" coefficients downgrade rather than pass
- Decision affected: D15 / NUM §2.4 (forward cone over the derivative-numerator coefficients; NUM §1.2's 64·eps roundoff floor).
- Evidence: implementer found that a 64·eps floor never terminates on a curve whose t=0 tangent is genuinely 63° off-chord at relative magnitude 4.5e-13 (a real property of the curve, not roundoff) and substituted a 1e-9 relative floor that SKIPS such coefficients; Astra break F1 (verified by recomputation) shows a skipped coefficient can hide an 89.87° tangent excursion at t=1e-6 while the chord certificate (0.018 mm) still holds; local review N1 shows the convexity argument does not cover skipped coefficients.
- Narrow correction: only exactly-zero coefficients may be excluded; any nonzero coefficient below the "too small to test" floor downgrades the span's angular status to `Undefined` (position stays certified; the leaf is accepted on the chord certificate per NUM §2.4's "keep a certified chord bound if available"); `Undefined` is acceptable at any subdivision boundary, recorded as an explicit singular vertex; `Satisfied` now means every nonzero coefficient lies in the cone.
- User-visible: none intended (positions unchanged); angular metadata becomes honest, which WP10 publishes on the wire.
- Wire/persistence: none in v1.
- Tests: TEST-CURVE-05 extended with the Astra quadratic and the two cubics (interior cusp, straight-locus root).
- Status: round 2 landed the downgrade rule. Astra followup (call 2/3, `docs/design/astra/wp08-curve-sampler-followup.md`) derives the actual conditions: `Satisfied` iff `β + asin(E/g) ≤ tol/2` with `g = p_min·s_min` (retained Bernstein mass) — the 1e-9 floor has no derivation and must be replaced by coefficient error bounds + cone margin; the round-2 resolution floor `max(16ρ, budget/1024)` gives NO crease guarantee (a 0.05 mm quarter-circle at X = 1e9 is emitted as one chord with 45° turns) — the display turn predicate `angle(P32[i]−P32[i−1], P32[i+1]−P32[i]) ≤ tol` at every regular join must become the ACCEPTANCE rule, with the positional floor used only to bound an unresolved singular region. **Round 3 owed** (not dispatched — implementation paused for handoff).

### DEV-WP08-2 — float32 representation allowance: per-axis ulp bound, measured quantization, no endpoints-only replacement
- Decision affected: NUM §1.2 (quantization error is measured, not a guessed ULP multiple) and the WP08 integration policy.
- Evidence: Astra F4/F5 (verified): the blanket `maxAbs·4·2^-23` bound is 4.62× above the true three-axis half-ulp bound and is wrong per axis (a YZ circle at X=1e9 quantizes below 1e-6 mm yet was replaced by two endpoints, a 30 mm loss); a short segment at 1e5 mm rotates 71.6° after encoding while passing the positional allowance.
- Narrow correction: per-span, per-axis half-ulp bound from the span's pole extents; measured float32 rounding error of every emitted point recorded as `quantizationErrorMm`; when representation alone exhausts the budget the double-certified polyline is emitted and labelled `QualityLimited` with the measured value (never endpoints-only); leaves whose chord is shorter than 16·ρ carry `Undefined` angular status. The doubling ladder is reserved for genuine work caps.
- User-visible: far-from-origin bodies keep curved edges (labelled limited) instead of straight chords; WP10 local origins remove the limit.
- Wire/persistence: none in v1; the measured value is the input WP10's `QUALITY_INFO.quantizationErrorMm` needs.
- Tests: X=1e9 circle through MESH1; 1e5 short line angular downgrade; S-curve at x=5000 Certified without a doubling.
- Status: round 2 landed (X=1e9 circle 129 points, quantization 5.1e-7 mm). Astra followup keeps F5 open: the `chord ≥ 16 × measured rounding` rule still admits a 7.125° rotation ((99999.997,0)→(100000.003,0.048) — recomputed); the cone must be tested against the ENCODED chord (or the measured rotation added to the angular bound). Also open from the followup: F6 (a normalisation underflow — weights 1e-300/1e300 → NaN pole — yields a zero hull bound through `std::max`; every generated control net must be validated finite, and the positional certificate needs the derived roundoff enclosure ≤ 4.3e-4 mm at 1e9 mm / degree 32 / depth 32), F7 (the `period − 1e-9` closure shortcut at `CurveSampler.cpp:557,920` is not semantic closure), F9 (post-budget endpoints still consume segments). **Round 3 owed.**

### DEV-WP03-1 — degraded highlight mode is a whole-body tint, not "body outline plus selection count"
- Decision affected: spec §8.3 last paragraph (over-cap exact overlays → keep semantic selection, one diagnostic per change, body OUTLINE plus selection COUNT while bounded exact overlays are prepared).
- Evidence: WP03 adversarial review B-2 (probes A8/B2): when the highlight cache cannot admit a selection with only pinned entries left, the layer keeps the semantic selection and emits one diagnostic per change (both per spec) but renders the whole-body translucent tint (`selBodyMat`) instead of an outline + count, the degraded display is STICKY (re-pushing the same selection after the budget frees keeps the tint because `rebuild()` short-circuits on displayed keys), and the public `HighlightLayer.degraded` getter reads false while the display is still degraded.
- Narrow correction (round 2, owed): (a) degraded rendering = the leased body outline (edge geometry, dashed selection style from `LINE_WIDTHS_CSS`) plus a selection-count chip through the existing overlay chip layer; (b) re-attempt the exact overlay on every rebuild while degraded (drop the displayed-key short-circuit for degraded keys); (c) `degraded` reports the display state, not the last reservation outcome.
- User-visible: only under a 64 MiB / 256-entry overlay budget breach (very large multi-face selections).
- Wire/persistence: none.
- Tests: TEST-RES-03 extended with a "degraded then recovers" case and an outline+count assertion.
- Status: open — WP03 round 2.

### DEV-WP03-2 — degraded selection outline is SOLID, not dashed
- Decision affected: spec §8.3 last paragraph / DEV-WP03-1 wording ("dashed selection style").
- Evidence: T4 (2026-09-14). A dashed `LineMaterial` draws dashes only when the geometry carries `instanceDistanceStart/End`; the only producer is `LineSegments2.computeLineDistances()`, which writes two new attributes and an interleaved buffer onto the geometry it is called on — here the LEASED registry geometry. Mutating a borrowed resource with untracked buffers is the defect class WP03 removes; `dashed: true` without the attributes silently draws solid.
- Narrow correction: the degraded display is the leased body outline at the `selectionHalo` weight (3.0 CSS px) in the selection token colour plus the count chip; still unmistakably distinct from the 1.25 px feature edges and from the rejected whole-body tint. A dashed variant needs an OWNED outline copy (WP13, with `applyScreenLineStyle`).
- User-visible: only under a highlight-budget breach.
- Wire/persistence: none.
- Tests: `HighlightLayer.test.ts` degraded cases (outline + count, edgeless body, recovery, moved anchor).
- Status: accepted 2026-09-14; DEV-WP03-1 items (a)–(c) closed by T4 + T4-fix.

### DEV-WP08-3 — round 3 acceptance rule and its two refuted shortcuts
- Decision affected: D10 / NUM §2.4–§2.5 (C1 turn predicate as the acceptance rule; C2 cone with retained mass).
- Evidence: Astra `verify` (`docs/design/astra/wp08-curve-sampler-verify.md`) and adversarial review R1(c) (2026-09-14): (i) suppressing an observed emitted-turn violation because the quantization-conditioning number is large is fail-open — the emitted turn is exact on the emitted points (r = 0.1 mm circle at X = 8192 mm published `Certified` with 180° turns); (ii) `reversalPresent` is not singularity evidence (regular cubic with sign-changing degree-5 coefficients); (iii) the F6 enclosure does not cover subnormal homogeneous coordinates.
- Narrow correction (T5-fix): the turn is measured and reported at every regular join and a violation blocks `Certified`; the floor+reversal branch publishes `Undefined` without a singular label or a turn exemption; underflowing homogeneous products refuse certification; J carries an interval; the pole enclosure is charged into the C2 `E`.
- Status: fix round in progress at handoff time; closed when the WP08 record below says so.

### RISK-PRE-1 — undefined `effectiveGeneration` window after a document switch (pre-existing)
- `meshSync.loadBody` computes `effectiveGeneration = undefined` when `currentPublication` is null or belongs to another document, so neither fence runs in the window right after a document switch. Pre-existing; WP04 widens its blast radius only in that a malformed payload could demote an installed body there. Routed to WP07 (pick freshness) / WP10 (publication identity).

## 7. Package handoff records

_(appended per package in the format of guide §23)_

## WP01 — metrics and CSS-line units
Status: focused-pass (U); G rerun owed
Source checkpoint: `65b4c60` + uncommitted WP00/WP01/WP04(in flight) tree; WP01 files listed below
Owned/changed files: new `src/viewport/engine/{ViewportMetrics,screenLineStyle,dprWatcher}.ts` (+ tests); modified `engine/{dpr,bodyMaterials,HighlightLayer,SketchObject,SketchStaticLayer,SnapIndicator,OriginTriad,Picker,ViewportEngine}.ts`, `engine/README.md`, colocated tests, one case in `src/viewport/vph/baselineCounterexamples.test.ts`; e2e `vph-line-width.spec.ts` annotations removed by the orchestrator
Requirements and findings: VP03 (spec §7), R01; TEST-LINE-01 (U), TEST-LINE-03 (U), TEST-LIFE-04 (U, watcher part)
Behavior changed: every `LineMaterial` width is CSS px and every application `resolution` write is logical px (one constructor site, `screenLineStyle.createScreenLineMaterial`); edge pick threshold `max(0, 2·6 − widthCss)`; one immutable `ViewportMetrics` snapshot written only by `resize()`/`syncDpr()`; idle DPR change detected by a re-armed `(resolution: Ndppx)` media query and repainted once with no camera change; `cssToDevice`/`cssLineWidth`/`applyDpr` deleted. Spec §7.3 value changes: sketch selection halo 2.5→3.0, draft preview 1.0→1.25, dimension line 0.75→1.0 (all other families unit-only). Points (`sizeAttenuation:false`) confirmed already CSS-sized (`WebGLMaterials.js:307` multiplies `size` by the renderer pixel ratio) — unchanged.
First failing reproduction: U — `baseline/vph-counterexamples-red.log` (`cssToDevice(1.25,2)=2.5`); G — `baseline/vph-g-lane-probes-at-65b4c60.log` (DPR 2 body edge 2.25 CSS px vs 1.25 authored)
Passing focused gates: orchestrator `bunx tsc --noEmit` exit 0; `bunx vitest run src/viewport/engine src/viewport/vph` **41 files / 623 passed / 7 expected fail** after the review fixes; reviewer's independent full `bunx vitest run`: 350 files / 6102 tests: 443 failed (identical pre-existing set) / 5574 passed / 7 expected fail / 78 skipped; hex gate empty
Integrated/native evidence: not-run (Playwright rerun of `e2e/vph-line-width.spec.ts` owed once the heavy lane frees; N lane not run)
Resource/performance delta: none measured (no allocation path changed)
Unresolved risks: DPR-1 baseline measured 1.00 CSS px vs 1.25 authored — exactly at the 0.25 tolerance (§3.1); a red there after WP01 needs a measurement-method review, not a WP01 revert. TEST-LINE-01 variants at DPR 1.5, dark theme and perspective, TEST-LINE-02 (G), TEST-LINE-04 not implemented. `applyScreenLineStyle` has no production caller yet (kept for WP13's dashed hidden-edge style).
Next allowed package: WP02 (dependency WP00 satisfied; owner file `ViewportEngine.ts` released by WP01), WP03/WP06/WP11 (dependency WP01 satisfied)

## WP02 — scheduler, submission truth, and context recovery
Status: focused-pass (U); G/N owed (TEST-LIFE-03, TEST-BACKEND-02)
Source checkpoint: `65b4c60` + uncommitted program tree
Owned/changed files: new `src/viewport/engine/FrameScheduler.ts` (+ test); modified `engine/ViewportEngine.ts`, `engine/renderer.ts`, `src/viewport/ViewportRoot.tsx` (lifecycle hint + `RendererRetryAction`), `src/features/settings/SettingsModal.tsx` (+ new test), `engine/README.md`, R06 case in `vph/baselineCounterexamples.test.ts`; logs `runs/wp02-focused/vitest.log`
Requirements and findings: VP01–VP02 (spec §5–§6), NUM §11.1, D02/D04, R06, R17; TEST-LIFE-01/02/04/05, TEST-BACKEND-01/03 (U)
Behavior changed: dirty-reason bitset consumed BEFORE frame work (no lost re-entrant invalidation; the R06 ratchet flipped green for the confirmed reason); one outstanding rAF; a tween of N update ticks renders N frames then idles (camera invalidations raised inside `controls.update()` do not re-dirty); `FrameSubmission {submission, requestedRevision, publication, displayedProvenance}` derived from the DISPLAYED entries' provenance (null when mixed) and handed to `onAfterRender` listeners — "submitted", never "presented"; renderer throw/rejection acknowledges nobody, identical messages throttled to one per 5 s, three consecutive failures park the engine (`error`) — a parked engine abandons the tween and re-renders only on an external invalidate or `retryRenderer()` (measured 3 attempts per tween on a throwing renderer, 23 attempts for 23 external invalidates, 1 log); `FrameScheduler.tick` restores consumed reasons on a throwing work and stops rescheduling after 3 consecutive throwing ticks; capabilities (`attributesActual`, depth/stencil bits, samples, max texture/renderbuffer, `reversedDepthSupported` probed not enabled, `depthConvention: "conventional"`) read once and logged at init; `outputColorSpace` explicit; `experimentalWebGpu` never selects WebGPU (one warn per session, `backendNote`), SettingsModal toggle disabled with helper text, store shape unchanged; lifecycle `constructing→active→lost→restoring→active|error|disposed` with a sticky hint on `lost/restoring/error`, a "Retry renderer" action in the viewport overlay (`retryRenderer()` re-runs full init when construction failed; fresh canvas, listeners rebound, camera carried), `captureThumbnail()` null unless `active`, double `restored` ignored, init tail respects `lost`.
First failing reproduction: U — `baseline/vph-counterexamples-red.log` (R06: frameCount stays 1); round-2 reds recorded in the reviewer's probes (86 draws across 5 tweens; 7 renders for a 6-tick tween; second listener after dispose)
Passing focused gates: orchestrator `tsc` exit 0; `bunx vitest run src/viewport/engine src/viewport/vph src/viewport/ViewportRoot.test.tsx src/features/settings src/ipc/promote.test.ts` **45 files / 710 passed / 6 expected fail**; hex gate empty
Integrated/native evidence: not-run
Resource/performance delta: `displayedProvenance` allocates one small array per submitted frame (worth a look at 1,000 bodies)
Unresolved risks: a parked tween snaps to its end on retry (abrupt, correct); `meshSync.traceRendered` wording still says "rendered" for a submission ack (WP03 owns `meshSync.ts` narrowly now — rename in WP07/WP12); reversed depth recorded, not enabled (no test lane yet); `allowUnsupportedBackends` is `@internal`, unset by the app
Next allowed package: WP06 (camera/input; owner files `CameraRig.ts`, `CadOrbitControls.ts`, `cameraFit.ts`, `navInput.ts`, engine sketch-entry section), WP07 after WP06

## WP03 — resource leases and bounded highlights
Status: focused-pass (U) + G (hover plateau); round 2 owed (review conditions); N owed
Source checkpoint: `65b4c60` + uncommitted program tree
Owned/changed files: new `src/viewport/mesh/{renderResourceIdentity,faceSliceGeometry,highlightCache}.ts` (+ tests); modified `mesh/meshRegistry.ts`, `mesh/meshSync.ts` (three `handle.dispose()` calls + detach ordering), `engine/{HighlightLayer,BodyObject,SectionLayer}.ts` (+ tests), `vph/instrumentation.ts` (counters), `engine/README.md` (orchestrator rewrote the ownership section), `e2e/vph-hover-lifecycle.spec.ts` (orchestrator disarmed `test.fail`); matrix rows TEST-RES-01/02/03/05; logs `runs/wp03-focused/vitest.log`, `runs/wp03-focused/playwright-probes-after-wp03.log`
Requirements and findings: VP04 (spec §8), NUM §11.2, D05, R02; TEST-RES-01 (U+G), TEST-RES-02, TEST-RES-03, TEST-RES-05 (U)
Behavior changed: every installed mesh entry carries a `RenderResourceIdentity` (64-bit FNV over id tables + incidence, geometryRevision, qualityKey, format version, provenance); borrowers hold leases (`body` on the handle, `section` per stencil pair, `highlight:body`); `swap`/`remove` retire and `flushDisposals` disposes only unleased entries at a later frame boundary; open leases at close trip the leak tripwire with owner tags; whole-body highlights use the exact body geometry under a lease; face highlights are compact owned slices (positions + remapped index, one buffer per body/role, ×1.5 growth) in a 64 MiB / 256-entry LRU pinned while displayed and dropped on source retirement; `shareIndexed` removed from `HighlightLayer`; a detached-borrower sweep in `BodyObject.ts` releases the engine's undisposed preview handles (deviation, see round 2).
First failing reproduction: G — `baseline/vph-g-lane-probes-at-65b4c60.log` (renderer geometries 11 → 611 over 300 hovers); U — the replaced `HighlightLayer.test.ts` case that asserted "never disposes a face overlay's geometry"
Passing focused gates: orchestrator `tsc` exit 0; `bunx vitest run src/viewport src/ipc/promote.test.ts` **59 files / 969 passed / 6 expected fail**; matrix verifier exit 0; reviewer probes: 1,000 alternating hovers hold owned bytes at 96 with zero `BufferGeometry.dispose` calls, edge overlay shares the `ArrayBuffer` but not the `InterleavedBuffer` (safe to dispose)
Integrated/native evidence: G — `runs/wp03-focused/playwright-probes-after-wp03.log`: 300 rendered hovers hold renderer geometries at **13** (11 → 13 → 13 …); N not-run
Resource/performance delta: hover plateau; the sweep is O(live handles) per retirement (N-2)
Unresolved risks (round 2): DEV-WP03-1 degraded mode; N-1 comments; N-2 four `ViewportEngine.ts` preview sites lack `handle.dispose()` (fixing them removes the sweep); N-3 section lease by name not object; N-4 estimate-vs-capacity accounting (≤ one third of a slot over cap); N-5 multi-writer cache counters; N-6 non-publication identity uniqueness by call order; N-7 highlight rebuild inside `swap()` before selection reconcile (stale `meshSync.ts:733` comment); N-8 `GhostLayer.ts` still has an undisposed shared wrapper and an unleased borrower — R02 is not fully closed until it moves to the lease model; 100 preview apply/cancel cycles (TEST-RES-05) owed
Next allowed package: WP05 (appearance; depends on WP03), WP11 (depends on WP01+WP03)

## WP09 — surface normals and complete tessellation
Status: implementing (green focused gates; adversarial review and Astra `break` owed); N owed
Source checkpoint: `65b4c60` + uncommitted program tree (dirty-diff sha256 `9902241f8f31` (of `git diff -- . ':!TODO.md' ':!HANDOFF.md' ':!CURRENT_STATE.md'`, i.e. excluding the three root handoff ledgers, which change on every handoff edit; 40 tracked files / +4 967 / −746; 89 porcelain entries incl. untracked) at handoff)
Owned/changed files: new `worker/src/tess/SurfaceNormals.{h,cpp}`, `worker/src/tess/EdgeClassification.{h,cpp}`, `worker/tests/test_surface_normals.cpp` (1154 lines, 104 assertions); modified `worker/src/tess/Tessellate.{h,cpp}` (face loop → `compute_face_normals`, `TessellationCompleteness`, `edge_classes`, `face_solid_ordinals` on `BodyMesh`; redundant `mesher.Perform()` removed after an 18-row byte-identical measurement; edge loop and `tessellate_raw` untouched), `worker/CMakeLists.txt`, `worker/tests/CMakeLists.txt` (target `surface_normals`); logs `baseline/ctest-surface-normals-red.log`, `runs/wp09-focused/ctest.log`
Requirements and findings: VP10 (spec §14), NUM §7, §8.1–8.2 (internal data only), D10, R04, R11 producer completeness; TEST-NORMAL-01..06, TEST-MESH-06 (W)
Behavior changed: normals evaluated from the supporting surface at each node (`BRepAdaptor_Surface::D1`, location applied once by the adaptor), `n = normalize(orientationSign · sign(det A) · (S_u × S_v))`, winding reversed iff `reversed XOR det(A) < 0`; planes analytic; sphere poles analytic radial; singular nodes (sine floor 1e-8, span floor 1e-9 — both scale-free, derivations in the header) → facet spread ≤ 5° area-weighted (`TriangulationFallback`) else per-triangle split (`SingularSplit`); no usable facet → `Missing` and the degenerate triangles dropped (display counts now differ from export: sphere 434 vs 436); splits append vertices inside the face's block so face ranges and triangle order are unchanged; a nondegenerate face with no triangles → `allNondegenerateFacesCovered=false`, `missingFaces`, WLOG_WARN, body still published; edge classes computed (open/hard/tangent/seam/degenerate/nonmanifold/unknown; tangent only from G1 metadata or analytic equivalence plus a 3-sample ≤0.2° consistency check; measured 1° → hard, 0.05° without metadata → unknown) and per-face solid ordinals (closure via shells) — stored on `BodyMesh`, not on the wire (WP10).
First failing reproduction: `baseline/ctest-surface-normals-red.log` — 19 FAIL lines against the retired sampler
Passing focused gates: implementer 45/45 focused, full ctest 197/199 (only `feature_pattern`, `chamfer_reference_face`); orchestrator rerun of the focused pattern at handoff **45/45** (2026-09-13 19:57 UTC); stdout hygiene clean; no parity/canonical delta
Integrated/native evidence: not-run
Resource/performance delta: `compute_face_normals` 5.4× the retired loop on a torus (0.918 ms for 21 025 nodes, 44 ns/node ≈ +20 ms on a 1M-triangle body by extrapolation, not measured on F16/F17)
Unresolved risks: adversarial review + Astra `break` not yet run (the reviewer should attack: `kSurfaceD1RelativeNoise = 1e-12` engineering bound, UV-node-bbox span floor on tiny trimmed patches of large surfaces, 1e-12 triangle floors, the 0.2° crease band labelled unknown, 3-sample dihedral downgrade, sphere-pole sign from facets); `tessellate_raw` keeps triangle-average normals + `+Z` fallback + the mirrored-winding bug (export policy decision, spec §14); `BRepBuilderAPI_Transform` does not retain a mirror as a location (deviation 1 — the true det<0 case uses `Moved(TopLoc_Location)`); shared-triangulation hazard: `tessellate_body` replaces the `Poly_Triangulation` on the session's `TShape`s (safe only because every geometry verb runs on the single kernel lane) — WP12 must not add concurrent display jobs without isolating it; `test_tessellation_quality.cpp` reads section type 8 where it means 7 (pre-existing, its edge assertions are accidental)
Next allowed package: WP10 (protocol/origins; single cross-layer owner; consumes `completeness`, `edge_classes`, `face_solid_ordinals`, `quantizationErrorMm`)

## WP04 — validation, admission, and publication freshness
Status: focused-pass (U); X/N owed
Source checkpoint: `65b4c60` + uncommitted program tree (WP00/WP01/WP02/WP04/WP08)
Owned/changed files: new `src/viewport/mesh/{validateMesh,meshAdmission,meshMutations,previewMesh}.ts` (+ tests, `meshSyncAtomicInstall.test.ts`); modified `meshRegistry.ts`, `meshSync.ts`, `meshSync.test.ts`; preview call sites `src/tools/modelTools/ModelToolController.ts` (`applyPreviewBodies`) and `src/modules/library/placementController.ts` (`onPreviewResult`) now validate-or-skip; orchestrator: `src/ipc/promote.ts` gate + `promote.test.ts` case; `acceptance-matrix.json` rows TEST-MESH-01..05, TEST-PUB-01..02
Requirements and findings: VP05 (spec §9), NUM §10/§11.2, R11, R15 boundary
Behavior changed: only a branded `ValidatedMesh` (16 semantic codes + `lod`, checked arithmetic before any derived allocation, zero-copy) becomes GPU geometry; peak admission (`MeshAdmission`, 1024 MiB CPU / 768 MiB GPU, holds live with the installed entry); display states `current | stale-inspection-only | failed-initial | pending-replacement` with body-specific hints; a failed replacement keeps the previous geometry installed and non-promotable (`isEntryPromotable`, enforced in `installedProofIsCurrent`); install is atomic (scene handle built before `swap`); post-install failures never demote; MESH1 v1 header bbox enclosure is MEASURED (excursion + measured bounds used for `geometry.boundingBox`), strict for v2 later; preview lanes skip an invalid preview body with a throttled warn instead of throwing.
First failing reproduction: none pure (R11 is a missing boundary); adversarial sweep and red probes recorded in the implementer report (three deliberate breakages each turned the named test red)
Passing focused gates: orchestrator `tsc` exit 0; `bunx vitest run src/viewport/mesh src/viewport/vph src/ipc/promote.test.ts` 11 files / 212 passed / 6 expected fail; `node scripts/verify-viewport-acceptance.mjs` exit 0; log `runs/wp04-focused/vitest.log`
Integrated/native evidence: not-run
Resource/performance delta: validation 10.36 ms for a 60 000-face body with all ids decoded vs 2.20 ms with one (DEV-WP04-1); mutation sweep 10 000 cases 63 ms
Unresolved risks: DEV-WP04-1 (contract not yet in `mesh_format.md`), RISK-WP04-2 (real worker bbox), RISK-PRE-1; preview/ghost entries validated but not admitted against the document budget (needs `ModelToolController`/`placementController` ownership — WP13/WP14); `colorBytes` over-estimates for all-unset authored colors (safe direction); `Picker.ts` still resolves picks on stale entries for hover (inspection allowed by spec) — promotion is where the gate bites
Next allowed package: WP07 (needs WP06), WP08 (already running), WP09/WP10 chain

## WP08 — exact-span curve sampler
Status: focused-pass (W); Astra followup on two policy constants pending; N owed
Source checkpoint: `65b4c60` + uncommitted program tree
Owned/changed files: new `worker/src/tess/CurveSampler.{h,cpp}`, `worker/tests/test_curve_sampler.cpp`; modified `worker/src/tess/Tessellate.cpp`, `worker/CMakeLists.txt` (source list), `worker/tests/CMakeLists.txt` (target `curve_sampler`); logs `baseline/ctest-curve-sampler-red.log`, `runs/wp08-focused/{ctest.log,ctest-round2.log,ctest-full-orchestrator.log,cargo-edge-consumers.log}`; Astra record `docs/design/astra/wp08-curve-sampler-break.md`
Requirements and findings: VP11 (spec §15), NUM §1–§2, D15/D16, R03; TEST-CURVE-01..07 (W)
Behavior changed: edges are sampled from exact positive-weight rational Bézier spans (lines, Béziers, B-splines incl. periodic unrolling, conics incl. shifted full periods) with a convex-hull finite-chord certificate and a derivative-cone angular criterion; only exactly-zero coefficients may be dropped, any sub-floor nonzero coefficient withdraws the angular claim (`Undefined`); stationary points/cusps are accepted as explicit singular vertices once the span is below a resolution floor; work caps (depth 32 / 262 144 per edge / 2 000 000 per body) publish nothing and are labelled `QualityLimited`; unsupported families go through `GeomConvert_ApproxCurve` and are labelled `KernelEstimated` with an independent check; float32 representation is bounded per span per axis and MEASURED per point (`quantizationErrorMm`), representation-limited edges keep their double-certified polyline; closure snapping requires semantic closure; degenerate/failed edges keep zero-point ranges with a named reason; TopExp order and ids unchanged; MESH1 bytes unchanged except `EDGE_POSITIONS` contents.
First failing reproduction: `baseline/ctest-curve-sampler-red.log` — old sampler emitted 2 points for the NUM §2.6 S-curve, 1.125 mm at t=0.25; round-2 red list (33 cases) in `ctest-round2.log`
Passing focused gates: orchestrator `ctest -R "curve_sampler|tessellation_quality|wp5_mesh1|parity|canonical"` 43/43 (round 1); orchestrator full `ctest --test-dir worker/build` **196/198** (only `feature_pattern`, `chamfer_reference_face` — baseline); orchestrator `ONECAD_REQUIRE_WORKER=1 cargo test --no-fail-fast` over `step_import_gate, project_edges, preview_edge_shell, topology_rebind, m2_gate, real_worker_smoke, breadth_ops`: **45 passed / 3 failed**, the three (`shell_box_open_top`, `shell_preview_matches_the_commit`, `shell_open_face_rebind_preserves_typed_evidence`) all in the baseline red set; stdout hygiene clean
Integrated/native evidence: not-run (N lane; full cargo workspace at the commit boundary)
Resource/performance delta: segment counts ~1.5× worst case vs the retired sampler (96 vs 128 for a full r=5 fine circle is fewer; 192 vs ~128 for r=15 at 0.01 mm is more); S-curve 151 points at 0.05 mm; max turn between emitted segments 4.64° (5° tier) after the resolution-floor correction (was 67.4° with a whole-budget floor — caught by a new segment-turn oracle)
Unresolved risks: ROUND 3 OWED per the Astra followup — (1) replace the resolution-floor acceptance with the display turn predicate at every regular join (exempt only independently established singularities; positional floor bounds the unresolved region only); (2) replace the 1e-9 test floor with the derived `Satisfied` condition `β + asin(E/g) ≤ tol/2` using retained Bernstein mass and coefficient error bounds; (3) test the cone against the ENCODED chord (F5); (4) validate every generated control net finite and charge the derived roundoff enclosure to the positional certificate (F6); (5) semantic closure only — remove the `period − 1e-9` shortcut (F7); (6) hard body segment cap incl. endpoints (F9); required tests listed in the followup record §4; hull certificate 2–5× conservative; conic leaf parameters are an affine image of the converted domain; periodic unrolling capped at 64 periods; cancellation parameter unwired until WP12; body-budget exhaustion path untested at 2 000 000 segments
Next allowed package: WP09 (normals; may begin), WP10 (after WP09)

## 8. End-of-session handoff — 2026-09-13 (session 1 of the program), implementation PAUSED by user for a fresh session

> **Committed 2026-09-13 (user-directed):** commit `67331740` on `viewport-hardening`, pushed to origin; branch and worktree renamed from `OneCAD-ai-agent`. This is a checkpoint commit, not a gate boundary — the L3 rung is still owed (see below). The paragraphs that follow describe the tree at the moment of the handoff, before the commit.

**Checkpoint.** HEAD `65b4c60eb226a201f2fa3eb565d7283b1c63b5ac` on branch `viewport-hardening` (worktree `/Users/andrejvysny/workspace/viewport-hardening`); 87 porcelain entries, all program-owned; `git diff --stat` 41 files / +4 986 / −746 (tracked) plus new untracked files; dirty-diff sha256 `9902241f8f31` (of `git diff -- . ':!TODO.md' ':!HANDOFF.md' ':!CURRENT_STATE.md'`, i.e. excluding the three root handoff ledgers, which change on every handoff edit; 40 tracked files / +4 967 / −746; 89 porcelain entries incl. untracked). **Nothing committed, nothing pushed** (not authorized). Worker binary `worker/build/onecad-worker` sha256 `d3b5e01312788db4…` (WP09 build), staged to `src-tauri/binaries/`. No heavy process left running; no owned Vite server. Port 4177 is held by another session's Vite for the main checkout — always pass `E2E_PORT=4179`.

**Completed code (all uncommitted, all reviewed unless noted).**
- WP00 — ledger, baseline gates, R01–R18 map, ratchet tests (`src/viewport/vph/baselineCounterexamples.test.ts`, now 6 `it.fails`: R07×2, R09×2, R10, R18), instrumentation, acceptance matrix + verifier, fixture catalog, ADR-0015, G-lane probes with baseline evidence at exactly `65b4c60`.
- WP01 (U focused-pass, G green) — CSS line units, `ViewportMetrics`, DPR watcher; reviewed, 8 non-blocking fixed.
- WP02 (U focused-pass) — `FrameScheduler`, submission truth, capabilities, WebGPU gate, lifecycle + retry; reviewed twice, 3 blockers + 13 non-blocking fixed.
- WP03 (U focused-pass, G hover plateau) — leases, owned face highlights, bounded cache; reviewed: sound with conditions, round 2 owed (see WP03 record).
- WP04 (U focused-pass) — `ValidatedMesh`, admission, display states, atomic install, promotion gate, preview validate-or-skip; reviewed, 2 blockers + M/N fixed.
- WP08 (W focused-pass) — exact-span curve sampler; local review + Astra `break` + Astra `followup`; round 2 landed; **round 3 owed** (see WP08 record and `docs/design/astra/wp08-curve-sampler-followup.md`).
- WP09 (W green, **unreviewed**) — surface normals, completeness, internal edge classification.

**Measured gates at handoff (orchestrator-run).** `bunx tsc --noEmit` exit 0 · `bunx vitest run src/viewport src/ipc/promote.test.ts` 59 files / 969 passed / 6 expected fail · focused worker pattern (`surface_normals|curve_sampler|tessellation_quality|wp5_mesh1|wp6_meshexport|parity|canonical`) 45/45 · full ctest (WP08 tree) 196/198 and (WP09 tree, implementer) 197/199 — only the two baseline reds · Rust edge consumers 45/3 (baseline reds only) · Playwright chromium probes: line-width 3/3, hover-lifecycle plateau at 13 · hex gate 0 · matrix verifier 0 (26 focused-pass, 1 implementing, 100 not-started). **Not run this session:** full `bun run test` after the program's changes (baseline 443 pre-existing failures outside the viewport; reviewers' partial runs saw the same set), full `cargo test --workspace` after WP08/WP09 (only the edge-consumer targets), `bun run e2e` (both projects), any N lane. A commit boundary needs the L3 rung per CLAUDE.md.

**Owed before any commit.** (1) WP08 round 3 (Astra followup items C1/C2/F5/F6/F7/F9 + tests); (2) WP09 adversarial review + Astra `break`; (3) WP03 round 2 (DEV-WP03-1, N-2 `ViewportEngine.ts` preview `handle.dispose()` ×4 + drop the sweep, N-3, N-8 `GhostLayer` to the lease model); (4) full `ctest`, `ONECAD_REQUIRE_WORKER=1 cargo test --workspace --no-fail-fast`, `bun run test`, `bun run e2e` both projects with `E2E_PORT=4179`, fmt/clippy — compared against the baseline red sets in §5.1, not against green.

**Next packages in dependency order.** WP05 (appearance, R05/R14/R18 — flips the R18 ratchet; depends on WP03), WP06 (camera/input — flips R07 ratchets; depends on WP01), WP10 (MESH1 v2 + local origins + quality metadata; single cross-layer owner; protocol-auditor before landing; consumes WP08 `quantizationErrorMm`, WP09 `completeness`/`edge_classes`/`face_solid_ordinals`; resolves DEV-WP04-1), then WP07, WP11, WP12, WP13, WP14, WP15, WP16.

**Astra budget used.** WP08: 2 of ~3 calls (break + followup); WP09: 0 of ~3.

## 9. Session 2 handoff — 2026-09-14, consolidation pass (review PR-01…PR-12), PAUSED by the user

**Inputs.** `docs/01-PROGRESS-REVIEW.md` and `docs/02-CODING-AGENT-CORRECTION-BRIEF.md` (external review of `61dab320`). All twelve findings verified present at HEAD by direct source reads before any change. Plan of record: root `PLAN.md` (status `paused`; the Run log is the per-task evidence ledger; the previous UX plan is archived at `docs/plans/2026-09-11-ux-hardening-PLAN.md`).

**Checkpoint.** HEAD `61dab320` on `viewport-hardening`; **nothing committed**; 86 porcelain entries; `git diff --stat` 68 files / +8,338 / −1,873 (tracked) plus 18 untracked (new sources/tests, `docs/design/astra/wp09-surface-normals-break.md`, `docs/qa/viewport-hardening/{README.md,baseline/manifest.json,runs/consolidation-g-lane/}`, `scripts/record-viewport-run.mjs`); dirty-diff sha256 `113d5aa47671` (exclusion convention as §8). Worker rebuilt and staged by `scripts/build-worker.sh Release` this session (fresh configure after the worktree rename; Unix Makefiles). No heavy process left running. Port 4177 still belongs to another session — every Playwright run used `E2E_PORT=4179`.

**Per-finding status** (mechanism → change → orchestrator-run evidence; all `implemented + focused-pass`, none `integrated-pass` — L3 not run):

| PR | Production change | Orchestrator-measured |
|---|---|---|
| 01 | `promoteOne` deleted; `promoteViewportPick` (proof captured in `Picker.resolvePick`, checked before the wire call, before the `el_` path, after the reply), `promoteRef` fails closed, `promoteAuthoritativeRef` typed for history-repair / edge-op closure / offset closure (both now snapshot-fenced); proofs cleared on regen reconcile | `tsc` 0; focused vitest 106/106; agent baseline diff 443 reds before and after, 0 new |
| 02 | `commitSwap`/`commitRemove` (pure) + `notifyRetired` (isolated); COMMIT block with no call-out, NOTIFY after `installed = true`; body-loaded/display-state listeners isolated | `bunx vitest run src/viewport/mesh` 12 files / 249 → 250 → 267 passed across T3/T7/T6 |
| 03A | `meshPreparationPlan.ts` single layout+cost authority; reserve after colour resolution, before derived allocation; DEV `assertBuiltMatchesPlan`; both preview lanes admitted via `buildPreviewEntry` | `tsc` 0; `src/viewport src/ipc/promote.test.ts src/modules/library` 1091 passed / 6 expected fail |
| 03B | `MeshEntry.reservation` released by `entry.dispose()` (the one disposer); no swap-time release; previews reserve on the document singleton | as PR-02 row |
| 04 | `planFaceSetCapacity` prices ×1.5 growth up front; `reserve` receipt, `put` refuses over-receipt; degraded = leased body outline (solid, see DEV-WP03-2) + count chip, re-attempted every rebuild | `src/viewport/engine` + cache/slice tests 44 files / 715 passed |
| 05 | in-tick `invalidate()` deferred to the tail; park cancels the queue; `resetErrors()` on retry/restore; after-render listeners isolated + bounded | `FrameScheduler`+`ViewportEngine` tests 90/90 |
| 06 | preview handles disposed at 4 sites, sweep deleted; ghosts lease (`"ghost"`) or own (`buildTriangleRangeGeometry`), `shareIndexed` deleted; sections lease `userData.meshEntry` and refuse a mismatch | as PR-04 row; grep for sweep/`shareIndexed` empty |
| 07 | hard body cap → zero-point range + `budgetLimitedEdges`; doubling ladder → `QualityLimited` with requested/achieved fields | `ctest -R "curve_sampler\|…\|determinism"` 46/46 (T5), 47/47 (after T8); parity/canonical unchanged |
| 08 | `out.ok = allNondegenerateFacesCovered` + diagnostic through `PlanExecutor` per-step diagnostics; certificate-based `classify_face_degeneracy` (fail-closed; area threshold and `FaceScale` deleted); Astra F3–F11 closed (representation-derived derivative budget, `Unresolved` provenance, symmetric triangle predicate, trusted-metadata-only tangency, analytic pole sign, cone-apex split, no +Z emission, valence bound) | 47/47 focused; agent full ctest 197/199 (baseline reds only); stdout hygiene clean |
| 09 | C2 cone `β + asin(E/g) ≤ tol/2` with retained Bernstein mass; F5 encoded-chord publication cone; C1 encoded-turn acceptance (`turn ≤ tol + J`); F6 net validation + charged roundoff enclosure; F7 semantic closure only | as PR-07 row; C1 corners 45° → 2.966° |
| 10 | `scripts/record-viewport-run.mjs` (committed `manifest.json`/`tests.json`, ignored raw logs); verifier refuses `.log` evidence; `baseline/manifest.json`; README | verifier now exits 1 with 26 rows to re-point (expected until the focused gates are rerun through the recorder) |
| 11 | `traceRendered` acknowledges only a `FrameSubmission` containing the body with the installed provenance | `meshSync.test.ts` 61/61 (red shown by disabling the guards) |
| 12 | `kEdgeRanges = 7`, `kEdgePositions = 8`, bounds validation, hand-authored E=2 fixture | `ctest -R "tessellation_quality\|wp5_mesh1"` 2/2 |

G lane (orchestrator-run through the recorder, `runs/consolidation-g-lane/manifest.json`): TEST-RES-01 1,000 hovers — geometries 13 flat from i=100, 2 cache entries, 1 open lease (3.3 min); TEST-RES-05 — 100 preview cycles geometries 24 / leases 1 flat, 50 document cycles geometries 3 / leases 0 flat, tripwires none (5.4 min).

Astra: WP09 `break` call 1/3 DEFECTIVE, accepted record `docs/design/astra/wp09-surface-normals-break.md` (every number recomputed); WP08 `verify` call 3/3 packet ready (sha `058e3de6f7bd`) — **awaiting user approval**.

**Owed before any commit (order).** (1) A2 approval + run, accept/reject findings; (2) R1 adversarial reviews on fresh contexts — (a) T3+T6+T7+T9, (b) T2+T4, (c) T5+T8 — red-first fix rounds; (3) rerun the FE and worker focused gates through the recorder (`consolidation-fe-focused`, `consolidation-worker-focused`) and re-point the 26 matrix rows + add TEST-PUB-03 / TEST-MESH-06 / TEST-RES-05 rows to `focused-pass`; (4) ledger: §2 states (WP02/03/04/08/09 → reviews closed), §6 add DEV-WP03-2 (degraded outline is SOLID — `computeLineDistances()` would write untracked attributes onto the leased geometry), DEV-WP08-1/2 → closed by round 3 pending A2, §7 per-package records; (5) **rebuild `src-tauri/target/`** (pre-rename absolute paths break tauri's build script) then the full L3 rung vs the §5.1 baseline reds; clean-checkout verifier; commit (user-authorised, no push).

**Known new behaviour to watch at L3.** A nondegenerate face with no triangles now FAILS the body's tessellation (D11) — any corpus/regen fixture that used to publish with a silently missing face will surface here. Surfaces of revolution/extrusion/offset now yield `Unresolved` normals on every regular node (no error enclosure yet) — visible as fallback provenance counts, not as wrong geometry. `PreviewOp` reports "no preview mesh" for an incomplete body.
